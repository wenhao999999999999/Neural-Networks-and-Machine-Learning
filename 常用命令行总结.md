# 一、 Anaconda 常用命令行

### **1. 创建虚拟环境**

```bash
# 创建名为 myenv 的虚拟环境，指定 Python 版本（如 3.9）
conda create -n myenv python=3.9

# 可选：创建时安装常用包（如 numpy, pandas）
conda create -n myenv python=3.9 numpy pandas jupyter

# 创建路径时指定安装路径
 conda create --prefix D:\Users\zhang\anaconda3\envs\pytorch_gpu python=3.9

#查看当前配置的通道列表
conda config --get channels

# 删除目标通道（以 conda-forge 为例）
conda config --remove channels conda-forge

# 查看当前默认路径配置
conda config --show envs_dirs

# 添加新的默认路径
conda config --add envs_dirs D:\Users\zhang\anaconda3\envs

# 删除旧的默认路径
# 替换为你的旧路径（如 C:\Users\zhang\anaconda3\envs）
conda config --remove envs_dirs C:\Users\zhang\AppData\Local\conda\conda\envs


```

### **2. 激活虚拟环境**

```bash
# Windows
conda activate myenv

# Linux/Mac
source activate myenv

# 指定路径创建的环境，激活时需要使用完整路径或名称
conda activate D:\Users\zhang\anaconda3\envs\pytorch_gpu
```

### **3. 查看已安装的包**

```bash
conda list

# 或查看特定包的版本
conda list numpy

#检查pytorch是否安装成功
>>python
>>import torch 
>>print(torch.__version__) # 打印PyTorch版本号
>>print(torch.cuda.is_available()) # 检查是否支持GPU加速（返回True/False）

```

### **4. 安装新包**

```bash
# 通过 conda 安装
conda install matplotlib

# 通过 pip 安装（在虚拟环境已激活的状态下）
pip install scikit-learn

# 添加国外包
conda config --add channels conda-forge

#从 PyTorch 官方 Channel 安装最新稳定版（自动匹配 CPU/GPU 版本）
conda install pytorch torchvision torchaudio -c pytorch
#CUDA 11.8 版本
conda install pytorch torchvision torchaudio pytorch-cuda=11.8 -c pytorch -c nvidia
#避免使用国外源
conda install pytorch torchvision torchaudio

# 添加清华conda源（包含pytorch等常用库）
conda config --add channels https://mirrors.tuna.tsinghua.edu.cn/anaconda/pkgs/main/
conda config --add channels https://mirrors.tuna.tsinghua.edu.cn/anaconda/pkgs/free/
conda config --add channels https://mirrors.tuna.tsinghua.edu.cn/anaconda/cloud/pytorch/
# 设置搜索时显示源地址（可选，方便排查问题）
conda config --set show_channel_urls yes

conda clean -i  # 清理索引缓存（关键，确保使用新源）
conda clean -p  # 清理未使用的包（可选，释放空间）
```

### **5. 更新包**

```bash
conda update numpy
```

### **6. 删除包**

```bash
conda remove pandas
```

### **7. 导出环境配置**

```bash
# 激活当前虚拟环境后，导出当前环境到 environment.yml
conda env export > conda env export > D:\backup\myenv1.yml

# 从配置文件创建环境
conda env create --prefix D:\Users\zhang\anaconda3\envs\myenv1 --file D:\backup\myenv1.yml
```

### **9. 停用虚拟环境**

```bash
conda deactivate
```

### **10. 删除虚拟环境**

```bash
conda env remove -n myenv
```

### **11. 查看所有虚拟环境**

```bash
conda env list
# 或简写
conda info -e

# 查看当前虚拟环境的安装路径
conda info --base
```

### **12. 其他常用命令**

```bash
# 查看 conda 版本
conda --version

# 更新 conda
conda update conda

# 搜索可用包
conda search tensorflow
```

# 二、Git常用命令

### **1.执行克隆命令**：

```bash
#克隆默认分支
git clone https://github.com/ROHANNAIK/Sales-Prediction.git

#克隆指定分支
git clone -b gx-lh ssh://git@idp-gitlab-ssh.lj.cn:2289/operation-ddap/roc-decision-platform-algo.git
```

### 2.远程仓库相关命令

```bash
#查看所有远程仓库配置
git remote -v

#查看当前所在分支
git branch  

#当前分支与远程分支的关联关系
git branch -vv

#切换分支
git checkout main

#创建新分支并自动切换到该分支
git checkout -b LSTM

#将新分支推送到远程仓库
git push -u origin LSTM

```

### 3.github相关

```bash
#初始化本地仓库
git init

#查看当前关联的远程仓库名称
git remote -v

#关联远程仓库
#http
git remote add origin https://github.com/wenhao999999999999/Neural-Networks-and-Machine-Learning.git
#ssh
git remote add origin git@github.com:wenhao999999999999/Neural-Networks-and-Machine-Learning.git

#删除原有的远程仓库关联
git remote rm origin

#windows上查看SSH密钥
Get-Content $env:USERPROFILE\.ssh\id_rsa.pub -ErrorAction SilentlyContinue

#测试密钥是否添加成功
ssh -T git@github.com

```



# 三、Jupyter常用命令行

### 1.查看当前目录的绝对路径

```bash
pwd
```

### 2.切换到指定目录

```bash
cd /root/.jupyter/zhangwenhao
```

### 3.将虚拟环境注册为 Jupyter 内核

```bash
python -m ipykernel install --user --name=zhanjiang
```

4.启动Jupyter远程服务器

```bash
jupyter notebook --ip=0.0.0.0 --port=8888 --no-browser --allow-root --root_dir=/root/.jupyter

#URL
http://192.168.0.188:8888/tree
```



# 四、Linux命令行

### 1.**GPU**

```bash
#查看NVIDIA GPU版本
lspci | grep -i nvidia
```

### 2.系统类型

```bash
#确定操作系统类型
lsb_release -a
```

### 3.CPU

```bash
#查看CPU版本
lscpu
```

### 4.python

```bash
#查看python版本
python --version

#退出交互环境
exit()
quit()

#安装pytorch的GPU版本

```

### 5.文件

```bash
#查看所有文件，包含隐藏文件
ls -a

#查看根目录文件
ls /

#将本地 Windows 系统中指定文件夹下的所有内容上传到 Linux 服务器指定目录
scp -r "D:\WenHao\实习\湛江项目\github\lstm_zwh\*" root@192.168.0.188:/root/.jupyter/zhangwenhao

#上传单个文件到远程服务器
scp "D:\WenHao\实习\湛江项目\github\lstm_zwh\model.py" 192.168.0.188:/root/.jupyter/zhangwenhao
```

# 五、Pytorch

### 1.安装GPU版本

```bash
# 检查电脑全局是否安装CUDA
nvcc --version

# 确认电脑是否有 NVIDIA GPU，并且驱动正常
nvidia-smi

#  安装自带CUDA的GPU版本的pytorch
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121

# 检查是否安装成功
import torch
print(torch.cuda.is_available())   # True 表示可以用 GPU
print(torch.version.cuda)         # 查看 CUDA 版本
print(torch.cuda.get_device_name(0))  # 查看 GPU 型号

```

