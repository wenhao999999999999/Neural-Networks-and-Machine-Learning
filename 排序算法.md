# 一、排序算法

## 1.选择排序

```c++
// 选择排序
	// 维护一个 sortedIndex 指针，所有 < sortedIndex 元素都是已排序的，所有 >= sortedIndex 的元素都是未排序的。
	// 稳定性：不稳定
	// 时间复杂度：n**2
	// 空间复杂度：1
    void sort(vector<int>& nums) {
        int n = nums.size();
        int sortedIndex = 0;

        while (sortedIndex < n) {
            // 找到未排序部分 [sortedIndex, n) 中的最小值
            int minIndex = sortedIndex;  
            for (int i = sortedIndex + 1; i < n; i++) {
                if (nums[i] < nums[minIndex]) {
                    minIndex = i;
                }
            }
            // 交换最小值和 sortedIndex 处的元素
            int temp = nums[sortedIndex];
            nums[sortedIndex] = nums[minIndex];
            nums[minIndex] = temp;

            sortedIndex++;
        }
    }
```

## 2.冒泡排序

```c++
// 冒泡排序
	// 维护一个 sortedIndex 指针，所有 < sortedIndex 元素都是已排序的，所有 >= sortedIndex 的元素都是未排序的。
	// 对选择排序进行优化，每次找到最小值时，只交换相邻的逆序对儿，不会去碰值相同的元素，保证稳定性。
	// 稳定性：稳定
	// 时间复杂度：n**2
	// 空间复杂度：1
    void sort(vector<int>& nums) {
        int n = nums.size();
        int sortedIndex = 0;
        while (sortedIndex < n) {

            // 加一个布尔变量，记录是否进行过交换操作
            bool swapped = false;

            // 倒序遍历 nums[sortedIndex..]
            // 寻找 nums[sortedIndex..] 中的最小值
            // 同时将这个最小值逐步移动到 nums[sortedIndex] 的位置
            for (int i = n - 1; i > sortedIndex; i--) {
                if (nums[i] < nums[i-1]) {
                    int temp = nums[i];
                    nums[i] = nums[i - 1];
                    nums[i - 1] = temp;
                }
            }

            // 如果一次交换操作都没有进行，说明数组已经有序，可以提前终止算法
            if (!swapped) {
                break;
            }

            sortedIndex++;
        }
    }

```

## 3.快速排序

```c++
// 快速排序
	// 1.在 nums 数组中任意选择一个元素作为切分元素 pivot（一般选择第一个元素）。
	// 2.对数组中的元素进行若干交换操作，将小于 pivot 的元素放到 pivot 的左边，大于 pivot 的元素放到 pivot 的右边（换句话说，其实就是将 pivot 这一个元素排好序）
	// 3.递归地对 pivot 左边的数组和右边的数组重复上述步骤：寻找新的切分元素，然后交换元素，使得切分元素左侧都元素都比它小，右侧都元素都比它大（换句话说，其实就是递归的去把 pivot 左右两侧的其他元素排好序）
	// 4.递归地重复上述操作，直到所有元素都放到正确的位置。
	// 稳定性：不稳定
	// 空间复杂度：O(logn)
	// 时间复杂度： O(n)

    // 快速排序主函数
	void quickSort(vector<int>&nums, int left, int right) {
        if (left < right) {
            int pivot = partition(nums, left, right);
            
            quickSort(nums, left, pivot - 1);
            quickSort(nums, pivot + 1, right);
        }
    }

	// 快速排序分区函数
		// 返回分区元素索引
	int partition (vector<int>&nums, int left, int right) {
        int pivot = nums[right]; // // 选最右为基准元素
        int i = left - 1; // 维护指针 i ，i 指向“<= pivot 区间”的尾部
        for (int j = left; j < right; j++) {
            if (nums[j] <= pivot) {
                i++; // 插入元素位置
                swap(nums[j], nums[i]); // 把小于 pivot 的元素移动到左边
            }
        }
        
        swap(nums[i+1], nums[right]); // 把基准元素交换到它的最终正确位置
        
        return i+1;
    }
```

## 4.归并排序

```c++
// 归并排序
	// 分：把区间 [left, right] 一直对半劈开，直到子数组只剩 1 个元素（天然有序）。
	// 治：将两个已排序的小区间，线性地合并成一个更大的有序区间。
	// 合并保证稳定：遇到相等元素，先拿左边的（用 <=），保持相对顺序不变。
	// 时间复杂度： O(n log n)
	// 空间复杂度： O(n)

    // 合并两个有序数组
    /**
     * 将 nums[left..mid] 与 nums[mid+1..right] 这两个“各自有序”的区间，合并为一个整体有序的区间。
     * 核心技巧：双指针 + 临时数组 temp。
     */
	void mergeSort(vector<int>& nums, int left, int right) {
        // base case
        if (left >= right) return;
        
        int mid = left + (right - left)/2;
        
        mergeSort(nums, left, mid);
        mergeSort(nums, mid + 1, right);
        
        merge(nums, left, mid, right);
    }

	void merge(vector<int>& nums, int left, int mid, int right) {
        vector<int> temp;
        int i = left;
        int j = mid + 1;
        
        // 1) 按照升序合并了两个有序数组
        while( i <= mid && j <= right) {
            if (nums[i] <= nums[j]) {
                temp.push_back(nums[i]);
                i++;
            } else {
                temp.push_back(nums[j]);
                j++;
            }
        }
        
        // 2) 把左边数组没加完的元素加进去
        while (i <= mid) {
            temp.push_back(nums[i]);
            i++;
        }
        
        // 3) 把右边数组没加完的元素加进去
        while (j <= right) {
            temp.push_back(nums[j]);
            j++;
        }
        
        // 4) 把临时数组的值放到原数组中
        for (int k = 0; k < temp.size(); k++) {
            nums[left + k] = temp[k];
        }
    }
```

## 5.排序算法经典例题

### 1.LeetCode 347「前 K 个高频元素」

#### 解法一：哈希计数 + 小根堆（priority_queue）

```c++
#include <bits/stdc++.h>
using namespace std;

class Solution {
public:
    vector<int> topKFrequent(vector<int>& nums, int k) {
        // 1) 统计每个数的出现次数：num -> freq
        unordered_map<int, int> freq;
        for (int x : nums) {
            ++freq[x]; // 键对应元素，值对应次数
        }

        // 2) 小根堆，保存 (频次, 数值)
        //   比较器：频次小的优先（位于堆顶），便于超过 k 时弹掉最小频次
        auto cmp = [](const pair<int,int>& a, const pair<int,int>& b) {
            return a.second > b.second; // 频次小的优先
        };
        priority_queue<pair<int,int>, vector<pair<int,int>>, decltype(cmp)> pq(cmp);

        // 3) 遍历哈希表，把 (freq, num) 放进堆
        for (auto& e : freq) {
            pq.emplace(e.first, e.second);
            if ((int)pq.size() > k) {
                pq.pop(); // 只保留前 k 个最高频
            }
        }

        // 4) 取出堆中元素（此时堆内就是前 k 高频，但顺序是从小到大）
        vector<int> ans;
        ans.reserve(k); // 提前为 ans 预留存储空间，容量至少为 k，但此时 ans.size() 仍然是 0。
        while (!pq.empty()) {
            ans.push_back(pq.top().first); // 只要数值
            pq.pop();
        }
        // 如果想从高到低输出，可以反转（题目允许任意顺序，可不反转）
        reverse(ans.begin(), ans.end());
        return ans;
    }
};

```

### 6.默写区

#### 1.lambda表达式比较函数

```C++
auto cmp = [](const pair<int, int>& a, const pair<int, int>& b) {
    return a.first > a.first // 小堆顶
}
```

# 二、链表算法

## 1.反转链表

### 1.递归法

```C++ 
LinkedNode* reverseLinkedList(LinkedNode* head) {
    // base case
    if (head == nullptr || head -> next == nullptr) {
        return head;
    }
    
    LinkedNode* last = reverseLinkedList(head -> next);
    head -> next -> next = head;
    head -> next = nullptr;
    
    return last;
}
```

### 2.迭代法

```c++
LinkedNode* reverseLinkedList(LinkedNode* head) {
	LinkedNode* pre = nullptr;
    while (head) {
        LinkedNode* next = head -> next;
        head -> next = pre;
        pre = head;
        head = next;
    }
    return pre;
}
```

# 三、回文串算法

## 1.最长回文子串

**题意简介**：给定一个字符串 `s`，找到 `s` 中最长的回文子串。

**解题思路**：可使用中心扩展法或动态规划。中心扩展法枚举每个字符及字符间隙作为中心，向两边扩展寻找最大回文。由于时间复杂度较低，常用中心扩展法。动态规划方法通过判断 `s[i]==s[j]` 且内部子串是回文确定更长回文，时间复杂度 `O(n^2)`。

```c++
class Solution {
public:
    string longestPalindrome(string s) {
        // 初始化结果
        string res = "";

        // 遍历字符串的每个字符，找到以每个字符为中心的最长回文串
        for (int i = 0; i < s.size(); i++) {
            // 奇数长度回文串
            string s1 = palindrome(s, i, i);

            // 偶数长度回文串
            string s2 = palindrome(s, i, i+1);

            // 更新最长回文子串
            res = s1.size() > res.size() ? s1 : res;
            res = s2.size() > res.size() ? s2 : res;
        }

        return res;
    }

    // 输入一个字符串和中心索引，返回最长回文串
    string palindrome(string s, int l, int r) {
        int left = l;
        int right = r;
        while (left >= 0 && right < s.size() 
                && s[left] == s[right]) {
            left--;
            right++;            
        }
        // 注意：
        return s.substr(left+1, right - left -1);
    }
};
```

# 四、动态规划算法

## 1.动态规划概述

​	**动态规划（Dynamic Programming, DP）\**是一种通过把复杂问题拆解成更小的子问题并利用子问题的解来构建原问题解的算法思想[cloud.tencent.com](https://cloud.tencent.com/developer/article/2437715#:~:text=)。与分治不同的是，动态规划关注于\**重叠子问题**和**最优子结构**这两个性质：

- **最优子结构**：问题的最优解可以由其子问题的最优解构造而成[cloud.tencent.com](https://cloud.tencent.com/developer/article/2437715#:~:text=)。也就是说，如果一个问题的最优解包含了子问题的最优解，那么该问题具有最优子结构性质。典型例子如最短路径、0/1背包等，它们的整体最优解可由局部子问题的最优解组合得到。
- **重叠子问题**：在用递归求解问题时，会反复计算相同的子问题[cloud.tencent.com](https://cloud.tencent.com/developer/article/2437715#:~:text=例子1：斐波那契数列)。具有重叠子问题时，可以通过记忆化或填表（自顶向下或自底向上）来避免重复计算，提高效率。例如斐波那契数列、最长公共子序列等都会反复遇到相同子问题[cloud.tencent.com](https://cloud.tencent.com/developer/article/2437715#:~:text=例子1：斐波那契数列)。

​	只有当一个问题同时具备**最优子结构**和**重叠子问题**时，我们才可以考虑使用动态规划来解决它[blog.csdn.net](https://blog.csdn.net/llyzznn/article/details/123242410#:~:text=4)。判断一个问题能否用DP，通常看它是否要求**求最值**（最大/最小值，最多/最少，最长/最短）或**计数**（有多少种方案），并且能否找到**重复出现的子结构**。另外，还需要满足**无后效性**（状态转移后当前状态不受之前决策路径影响，只有当前子问题的状态有关），这通常隐含在最优子结构性质中。

**动态规划解题一般步骤**：

1. **定义状态**：找出原问题的子问题模式，确定如何用一个（或多个）变量描述任意子问题的状态。换句话说，明确“dp状态的含义”。通常通过分析问题最优解的**最后一步**或**子结构**来划分状态。
2. **状态转移方程**：根据子问题与原问题的关系，找出从更小状态转移到当前状态的公式，即确定**转移方程**（递推关系）。这一步往往是DP的核心，需要认真分析各种决策（例如“选或不选”[leetcode.cn](https://leetcode.cn/discuss/post/obCjFP/#:~:text=动态规划)、选择哪个选项等）对状态的影响。
3. **初始条件和边界**：确定最小子问题的解，即边界情况的状态值。通常根据问题定义直接给出，例如dp为0或1的情况。
4. **计算顺序**：决定计算子问题的顺序。可以采用**自顶向下**（递归+记忆化）或**自底向上**（迭代填表）两种方式。自底向上需要设计好循环嵌套顺序，确保转移时所需的子状态已被计算。

​	通过上述步骤，就可以写出DP解法的代码实现。下面我们将常见的动态规划面试题按**题型模式**分类讲解，包括如何识别这类问题、状态与转移设计、常用优化策略，以及每类中的高频LeetCode题目代表案例和解题代码。

## 2.常见动态规划题型分类

### 1.背包类动态规划

**特点与识别**：背包类问题的典型特征是给定若干物品（每个物品有一定“价值”和“成本”，如重量或体积），在限定条件下（如总容量或总重量限制）选择一些物品以达到目标（最大/最小价值，或精确填满等）。如果题目描述中出现了**“选择若干元素使得……达到最大/最小”**或者**“能否/有多少种选法使……满足某个和/容量”等**，往往可以用背包DP解法。背包问题满足最优子结构——在容量限制下选取最优解的策略可以由子容量的最优解递推得到，并且不同子容量的最优选取会重复出现，具备重叠子问题性质。因此这类问题很适合DP求解。

**状态定义**：常见定义有两种：

- 用二维状态`dp[i][j]`表示**前i个物品**在**容量为 j** 的限制下能达到的最优值（或布尔、方案数等，视问题而定）。这是直观的定义方式，其中状态包含物品索引和剩余容量两个维度。
- 优化空间后的一维状态：`dp[j]`表示**在容量为 j** 的情况下可达到的最优值（在遍历物品时逐步更新）。这种压缩状态利用滚动数组技巧，将对第i件物品的决策直接反映在容量维度上，但要求注意更新顺序。

**转移方程**：背包问题的基本转移理念是**“选或不选”**。以0/1背包为例，如果我们有第i个物品（重量`w_i`，价值`v_i`）：对于容量`j`：

- **不选第 i 件**：则价值和状态与前 i-1 件物品相同，即 `dp[i][j] = dp[i-1][j]`。
- **选第 i 件**：前提是容量足够放下它（`j >= w_i`），此时总价值为前 i-1件 在剩余容量 `j - w_i `下的最佳价值加上当前价值，即 `dp[i][j] = dp[i-1][j-w_i] + v_i`。

综合取最大值（或判断可行性)：
$$
dp[i][j] = \max\{\,dp[i-1][j],\;\; dp[i-1][j-w_i] + v_i\,\}
$$
 如果是判断是否可达到某总和或计数方案数，只需将状态定义和转移相应改成布尔值或加法计数形式。

对于**完全背包**（物品可无限次选取，比如硬币换钱问题），转移略有不同：如果允许重复选，则在计算选取时可以不必仅依赖 `i-1` 状态，而是可以**在 i 维度上重复使用**当前物品。常用实现是在一维压缩时改用**正序遍历容量**（从小到大），使得当前物品可反复选取。对应公式类似：
$$
dp[j] = \max\{\,dp[j],\;\; dp[j-w_i] + v_i\,\}~(完全背包求最值)
$$
 或
$$
dp[j] += dp[j-w_i] ~(完全背包求方案数)
$$
**优化策略**：背包DP常用**空间压缩**技巧：利用一维数组反向或正向更新，省去物品维度。对于0/1背包，必须**倒序遍历容量**（从`maxCapacity`下降到物品重量），以避免在同一轮次中重复使用同一物品；对于完全背包，采用**正序遍历**容量即可。此外，一些背包问题可以优化时间，例如利用位运算优化子集和求解等，但总体DP转移复杂度通常是`O(N*Capacity)`。根据具体题目范围，可权衡状态设计以降低复杂度。

**典型问题**：0/1背包及其变种在面试中高频出现。例如：

- 0/1背包基础问题：给定物品和容量，求最大价值。
- LeetCode 416. **分割等和子集** (Partition Equal Subset Sum) – 判断数组能否划分成两个等和子集（可抽象为0/1背包求是否能填满总和的一半）。
- LeetCode 322. **零钱兑换** – 求最少硬币数（完全背包求最小值）。
- LeetCode 518. **零钱兑换 II** – 求硬币组合数（完全背包求方案数）。
- LeetCode 494. **目标和** – 计算赋符号使得总和为目标的方案数（可转化为子集和计数问题）。
- LeetCode 474. **一和零** – 多维背包（两个“容量”维度，一个字符串的0和1数量限制）。

**示例代码：LeetCode 416 分割等和子集**（0/1背包判定问题，用C++实现动态规划，附中文注释）

```C++
bool canPartition(vector<int>& nums) {
    int sum = 0;
    for (int num : nums) sum += num;
    if (sum % 2 != 0) return false;        // 总和为奇数，无法平分
    int target = sum / 2;
    vector<bool> dp(target + 1, false);
    dp[0] = true;                         // 容量为0时表示什么都不选即可达成
    // 遍历每个物品（数组中的数）
    for (int num : nums) {
        // 0/1背包必须倒序遍历容量，以避免重复使用同一元素
        for (int j = target; j >= num; --j) {
            // 转移方程：选或不选当前数，dp[j]为真若之前已经能凑出j，
            // 或之前能凑出j-num现在加上当前num可以凑出j
            if (dp[j - num]) {
                dp[j] = true;
            }
        }
    }
    return dp[target];
}
```

上述代码使用一维滚动数组`dp[j]`表示容量`j`是否可达。倒序更新确保每个元素只能用一次。时间复杂度`O(N*target)`，空间`O(target)`。对于**价值型**背包问题，只需把布尔状态改为数值比较大小的状态，转移取最大即可。

### 2.序列型动态规划

**特点与识别**：序列型DP主要处理**线性序列**（如数组或序列本身）的动态规划。题目通常涉及在数组、数列或序列中做决策，如相邻元素不能同时取，或者需要计算以某元素为结尾/开头的某种最优属性。**线性DP**题目的子问题通常是前缀或前缀 + 一个元素的问题。这类问题容易识别：输入往往是一个序列（数组、字符串等），问题问的是**全局的最值或可行性**，且**决策过程沿序列推进**。典型例子包括爬楼梯问题、打家劫舍（不能取相邻）、最大子数组和、最长上升子序列等。

**状态定义**：通常使用一维状态`dp[i]`表示以序列**前 i  个元素（或下标i处）**为基础的某种最优值或属性。定义需要根据具体问题调整，比如：

- 对于最大子数组和，`dp[i]`可以定义为**以第 i 个元素结尾**的最大连续子数组和。
- 对于是否能跳到某位置，`dp[i]`可以定义为**能否从起点跳到位置 i**。
- 对于路径计数类，`dp[i]`表示到达位置 i 的方案数。

关键是明确`dp[i]`包含了哪个子序列的信息（前缀、后缀或以i为结尾/起点等）。很多序列DP的问题还需要结合**状态机**或**额外维度**来表示某些开关，例如打家劫舍II需要处理环形、股票交易需要处理持有与否等——这些本质上仍是沿序列推进。

**转移方程**：线性DP的转移通常按照序列索引推进，`dp[i]`从`dp`的某些前面索引的状态推导而来。常见转移模式：

- **相邻关系**：若问题限制某些相邻元素不能共存，比如打家劫舍，转移就与`i-1`或`i-2`有关。例：`dp[i] = max(dp[i-1], dp[i-2] + value[i])`，要么不取第 i 个（解与 i-1 相同），要么取第 i 个加上跳过前一个的最优解。
- **累积/递推**：如爬楼梯，`dp[i] = dp[i-1] + dp[i-2]`（来自前一步或前两步）；最大子数组和，`dp[i] = max(nums[i], dp[i-1] + nums[i])`（要么重新从当前元素开始，要么延续之前子数组）。
- **前缀比较**：如最长上升子序列 (LIS)，需要从`0..i-1`所有状态转移：`dp[i] = 1 + max{ dp[j] }` for all `j < i` with `nums[j] < nums[i]`。这是`O(n^2)`解，虽有更优解但DP易于理解。

**优化策略**：很多线性DP的空间复杂度可以优化为`O(1)`或`O(k)`，因为`dp[i]`常只依赖有限几个之前的状态。例如斐波那契、爬楼梯、打家劫舍等只需记住前一两个状态，用滚动变量代替数组即可。对于某些需要遍历之前所有状态（如LIS）的情况，可以尝试**二分查找优化**（LIS有O(n log n)解法）或其他数据结构优化，但那已超出DP表格范畴。面试中，先给出DP思路是主要的，优化可以通过提示进行。

**典型问题**：此类题非常多，也是算法面试常考：

- LeetCode 70. **爬楼梯** – 经典斐波那契型DP，dp[i] = dp[i-1]+dp[i-2]。

  ```C++
  // 目标：到达楼顶的方法数量
  // 状态：还有多少阶楼梯
  // dp[i] 表示爬 i 阶楼梯有多少种方案
  // 选择：爬 1 阶，或者爬 2 阶
  // 状体转移方程：
  	// dp[i] = dp[i-1] + dp[i-2];
  // base case:
  	// dp[1] = 1;
  	// dp[2] = 2;
  class Solution {
  public:
      int climbStairs(int n) {
          if (n == 1) return 1;
          if (n == 2) return 2;
          vector<int> dp(n+1, 0);
          dp[1] = 1;
  		dp[2] = 2;
          for (int i = 3; i <= n; i++) {
              dp[i] = dp[i-1] + dp[i-2];
          }
          return dp[n];
      }
  };
  ```

  

- LeetCode 53. **最大子数组和** – 一维DP求连续子段最大和（Kadane算法变形的DP解）。

  ```C++
  // 目标：连续子数组的最大和
  // 状态：以第 i 个元素结尾的连续子数组
  // dp[i]表示：以第 i 个元素结尾的连续子数组的最大和
  // 选择：
  	// 加以前一个元素结尾的最大子数组和  dp[i] = dp[i-1] + nums[i-1]
  	// 不加以前一个元素结尾的最大子数组和 dp[i] = nums[i]
  // 状态转移：
  	// dp[i] = max(nums[i], dp[i-1] + nums[i])
  // base case：dp[0] = nums[0]
  class Solution {
  public:
      int maxSubArray(vector<int>& nums) {
          int n = int(nums.size());
          if (n == 0) return 0;
          if (n == 1) return nums[0];
          
          vector<int> dp(n+1);
          dp[1] = nums[0];
          for (int i = 2; i <= n; i++) {
              dp[i] = max(dp[i-1] + nums[i-1], nums[i-1]);
          }
          
          int res = INT_MIN;
          for (int i = 1; i <= n; i++) {
              res = max(res, dp[i]);
          }
          
          return res;
      }
  };
  ```

  

- LeetCode 198. **打家劫舍** – 相邻不能同时偷，典型线性选择问题。

  ```C++
  class Solution {
  public:
      // 1.目标：偷窃到的最高金额
      // 2.状态：当前所处在的房间的编号 i 
      // 3.dp[i]定义：前 i 间房子中（下标 0...i-1），能偷到到最高金额。
      // 4.选择：
      	//偷第 i 家：那就不能偷第 i-1 家，所以加上 dp[i-2] + nums[i-1]
      	//不偷第 i 家：取前 i-1 间的最优，即dp[i-1]
      // 5.状态转移方程：
      	// dp[i] = max(dp[i-1], dp[i-2] + nums[i-1])
      // 6.base case:
      	// dp[0] = 0 (没有房子，金额 0 )
      	// dp[1] = nums[0] (只有一间房，只能偷这间)
      int rob(vector<int>& nums) {
          int n = int(nums.size());
          if (n == 0) return 0;
          if (n == 1) return nums[0];
          vector<int> dp(n+1, 0);
          // dp[0] = 0;
          // dp[1] = nums[0];
          
          // for (int i = 2; i <= n; i++) {
          //    dp[i] = max(dp[i-1], dp[i-2] + nums[i-1]);
          // }
          
          dp[0] = nums[0];
          for (int i = 1; i < n; i++) {
              dp[i] = max(dp[i-1], (i >= 2 ? dp[i-2] + nums[i] : nums[i]));
          }
          
          return dp[n-1];
      }
  };
  ```

  

- LeetCode 213. **打家劫舍 II** – 环形数组上的打家劫舍，可转化为两个线性情况取最优。

  ```C++
  // 环破为链：因为第 1 户和最后 1 户互斥，枚举两种不相交的线性区间，取两者最大值即可：
  	// 情况 A：只考虑下标 [0..n-2]（不偷最后一户）
  	// 情况 B：只考虑下标 [1..n-1]（不偷第一户）
  	// 答案是 max(robLine(nums, 0, n-2), robLine(nums, 1, n-1))。若 n==1，答案就是 nums[0]。
  // 目标：偷窃到最高金额
  // 状态：当前所在的位置 i 
  // dp[i]：以 i 户结尾所能盗取的最高金额
  // 选择：偷或不偷
  // 状态转移方程：dp[i] = max(dp[i-1], dp[i-2] + nums[i-1])
  // base case: dp[0] = nums[0]
  class Solution {
  public:
      int rob(vector<int>& nums) {
          int n = int(nums.size());
          if (n == 0) return 0;
          if (n == 1) return nums[0];
          
          // 情况 1 :不偷最后一家
          int case_1 = robList(nums, 0, n - 2);
          
          // 情况 2 :不偷第一家
          int case_2 = robList(nums, 1, n - 1);
          
          return max(case_1, case_2);
      }
      
      int robList(vector<int>& nums, int left, int right) {
          if (left == right) return nums[left];
          int m = right - left + 1; // 线性数组长度
          vector<int>dp(m, 0);
          dp[0] = nums[left];
          for (int k = 1; k < m; k++) {
              int i = left + k;
              int take = nums[i] + (k >= 2 ? dp[k-2] : 0);
              int skip = dp[k-1];
              dp[k] = max(take, skip);
          }
          return dp[m-1];
      }
  };
  ```

  

- LeetCode 300. **最长上升子序列** – 求序列中严格递增的最长长度，典型O(n^2) DP解法。

  ```C++
  // 目标：求最长递增子序列的长度。
  // 状态：以元素 i 结尾的递增子序列，用 i 表示结尾位置。
  // dp[i] 定义：dp[i] = 以元素 nums[i] 结尾的连续递增子序列的最大长度
  // 选择：在所有 j<i 且 nums[j]<nums[i] 的位置里选一个接上。
  // 状态转移：
  	//dp[i] = max(dp[i], dp[j]+1)
  // base case：dp[i] = 1，至少包括自己
  // 顺序：i 从左到右，j 从 0 到 i-1。
  class Solution {
  public:
      int lengthOfLIS(vector<int>& nums) {
          int res = INT_MIN;
          int n = int(nums.size());
          
          // dp[i] 表示以 nums[i] 元素结尾的最长子序列的长度
          vector<int>dp(n,1); // base case: 以 nums[i] 结尾的 LIS 长度为 1 
          for (int i = 0; i < n; i++) {
              for (int j = 0; j < i; j++) {
                  if (nums[j] < nums[i]) {
                      dp[i] = max(dp[i], dp[j] + 1);
                  }
              }
          }
          
          // 最终返回结果应该是 dp[i] 的最大值
          for (auto n : dp) {
              res = max(res, n);
          }
          return res;
      }
  };
  ```

  题目解析：

  ##### 1. 状态（state）

  在动态规划里，状态表示「一个子问题的独立刻画方式」。
   在 LIS 中，我们定义：

  ```
  dp[i] = 以 nums[i] 结尾的最长递增子序列的长度
  ```

  👉 所以 **状态就是下标 i**，它唯一决定了子问题：「以 `nums[i]` 结尾时，LIS 的长度是多少？」
   换句话说，状态就是「子序列的结尾位置」。

  ##### 2. 选择（choice）

  选择描述的是「从当前状态出发，可以怎么走」。
   在这道题中，如果我们要计算以 `nums[i]` 结尾的 LIS，有两种情况：

  - **不选任何前驱**：那么以 `nums[i]` 结尾的序列就是它自己，长度为 1（base case）。

  - **选择一个前驱 j（0 ≤ j < i 且 nums[j] < nums[i]）**：
     表示把 `nums[i]` 接到以 `nums[j]` 结尾的 LIS 后面，于是：

    ```
    dp[i] = max(dp[i], dp[j] + 1)
    ```

  👉 所以 **选择就是「选哪个 j 作为前驱」**。

  ##### 3. 状态转移方程

  把「状态」和「选择」结合起来，得到转移方程：

  ```
  dp[i] = max(dp[i], dp[j] + 1)   // 对所有满足 j < i 且 nums[j] < nums[i]
  ```

- LeetCode 91. **解码方法** – 字符串序列DP，dp[i]由前一位或前两位状态转移（类似爬楼梯但需检查有效性）。

- LeetCode 62. **不同路径** – 网格路径计数，可视为坐标DP但本质也是二维序列推进。

### 3.区间型动态规划

**特点与识别**：区间DP用于解决**涉及区段（区间）决策**的问题，即问题的输入往往是一个序列，但我们需要考虑序列中一个子区间（i到j）上的最优解，并通过**合并子区间**来得到更大区间的解。典型特征是题目问的是某个序列的**整体区间**的某种性质（最优值、能否成功等），而直接从前往后、后往前的线性决策不明显，需要考虑**任意起点和终点**的子序列决策。此类问题常见于：矩阵链乘优化、戳气球、字符串添加求回文、石子合并、棒切割等。这些问题通常提示我们考虑“把问题划分为两个子区间”或“考虑不同的分割点”。如果题目需要我们计算序列的某种全局性质且**通过划分区间或枚举区间长度来决策**，一般就是区间DP。

**状态定义**：通常使用二维状态`dp[i][j]`表示**在序列区间 i 到 j 上**所要求的最优值或属性（i和j通常是起止索引）。这里i、j定义了一个闭区间，要求**i <= j**。状态表示一个子序列区间的问题解，比如字符串s的i..j子串是否满足某性质，或在i..j范围进行某种操作的最优结果。边界一般是**区间长度为1或0**的情况：如i==j时区间内只有一个元素，很容易直接确定dp值。

**转移方程**：区间DP的转移需要**依赖更小的区间**。通常有两种思路：

1. **双端决策**：当问题决策体现在区间两端时，例如回文子序列问题，我们考虑区间两端字符是否相等；或博弈取数问题考虑取哪一端。此时转移关系往往从`dp[i+1][j]`或`dp[i][j-1]`推导到`dp[i][j]`。
2. **枚举分割点**：很多区间DP需要尝试**将区间 [i,j] 分为 [i,k] 和 [k+1,j] 两个子区间**进行处理，然后合并两个子区间的解。例如矩阵链乘积问题，选一个k作为最后一次相乘的位置；又如戳气球问题，选哪个气球最后被戳破。这类转移形式通常是：
    $dp[i][j] = \max/\min_{i \le k < j} \{\,dp[i][k] + dp[k+1][j] + \text{合并代价}(i,j,k)\,\}$
    或根据题意其他组合方式。即通过遍历所有可能的分界点k，来更新i..j的最优解。

因为区间状态依赖更小区间，为确保子状态已计算，一般按照**区间长度从小到大**来增加计算。我们可以两层循环：外层遍历子区间长度`len`从1到N，内层遍历起点`i`，计算出`j = i+len-1`。这样所有长度更短的子区间在计算长度=len的区间之前就已求出，符合转移要求[leetcode.cn](https://leetcode.cn/discuss/post/obCjFP/#:~:text=6)。此外也可递归+记忆化来自然地满足这一顺序。

**优化策略**：区间DP时间复杂度往往较高（通常O(n^3)因为三重循环：区间长度、起点、分割点）。针对特定问题，可利用**单调性优化**或**四边形不等式优化**等技巧降低到O(n^2)。但这些属于高级话题，需满足一定性质才能应用。在一般面试中，实现正确的O(n^3)解法已经不易。若n较大（如1000以上），有可能考查的是问题特性的特殊优化或贪心法，而非朴素DP。这需要具体问题具体分析，不属于通用DP框架。

**典型问题**：

- LeetCode 516. **最长回文子序列** – 典型区间DP，求字符串在任意区间内的回文序列长度，最终目标是整个字符串的LPS长度。
- LeetCode 312. **戳气球** – 经典高难度区间DP，求最大分数。需要枚举最后一个被戳的气球作为分割点。
- LeetCode 1000. **合并石头的最低成本**（或类似的石子合并题） – 需要考虑如何合并区间并计算代价，区间DP+贪心策略。
- LeetCode 132. **分割回文串 II** – 可利用区间DP预处理回文和划分子区间求最少分割次数。
- LeetCode 375. **猜数字大小 II** – 猜数游戏最坏情况下的最小代价，可视作区间分割问题（类似二分决策，也可区间DP）。
- 经典**矩阵链乘法**问题 – 给出矩阵相乘顺序，求最小乘法运算次数，是教科书中的区间DP示例。

**示例代码：LeetCode 516 最长回文子序列**（求字符串的最长回文子序列长度，区间DP实现）

```
int longestPalindromeSubseq(string s) {
    int n = s.size();
    if (n == 0) return 0;
    // dp[i][j] 表示字符串s在区间[i..j]之间的最长回文子序列长度
    vector<vector<int>> dp(n, vector<int>(n, 0));
    // 区间长度为1的情况（单个字符的回文子序列长度为1）
    for (int i = 0; i < n; ++i) {
        dp[i][i] = 1;
    }
    // 枚举子串长度 from 2 to n
    for (int len = 2; len <= n; ++len) {
        for (int i = 0; i + len - 1 < n; ++i) {
            int j = i + len - 1;
            if (s[i] == s[j]) {
                // 若两端字符相等，则成为回文的一部分
                dp[i][j] = dp[i+1][j-1] + 2;
            } else {
                // 不相等，则最长回文序列要么不包含i字符要么不包含j字符
                dp[i][j] = max(dp[i+1][j], dp[i][j-1]);
            }
        }
    }
    return dp[0][n-1];
}
```

在这段代码中，我们按照子串长度递增的顺序计算dp。当`s[i] == s[j]`时，将两端纳入回文，所以长度加2再加上中间子区间`s[i+1..j-1]`的解；否则就取掉一端看另一端区间谁回文更长。**注意**：当`len == 2`且两端相等时，我们会计算出2，这也包含在上述转移中了（因为dp[i+1][j-1]在i和j相邻时为dp[i+1][i]，根据初始化相当于0）。区间DP的难点在于考虑所有分割或决策方式，确保转移完整无遗漏。

### 4.股票交易类动态规划

**特点与识别**：股票问题是一类典型的动态规划题，特点是在一系列每日价格序列中进行多次买卖决策，以最大化利润。题目通常描述为：“给定一个股票价格数组，可以进行至多k次交易/任意次交易/含冷冻期/含手续费，求最大利润”。本质上，这类题是一个**决策序列问题**，但决策状态不仅取决于天数，还取决于**已经进行的交易次数**和**当前是否持有股票**。因此我们需要一个**状态机**思维，用DP模拟交易过程中的不同状态转变。识别这类问题并不难：凡是“股票”“买卖”“交易次数限制”“手续费”“冷冻期”等关键词的题，基本都是此类DP问题。

**状态定义**：股票问题常用**多维状态**来表示复杂情况。有一个经典的统一模型[blog.csdn.net](https://blog.csdn.net/llyzznn/article/details/123242410#:~:text=本文系统介绍了 动态规划 算法 的核心思想 和,0%2F1]表示第i天最多k次交易时的最大利润），并详细分析了不同变体的状态转移方程 和 代码实现，包括交易次数限制（1次、2次、k次）、无限制交易以及含冷冻期的情况。通过空间优化技巧，这些解法都能在O(n)或O(nk)时间内高效解决问题。)：
 设 `dp[i][k][0/1]` 表示**第i天**（通常i从0开始）**最多进行k次交易**的情况下**是否持有股票**所能获得的最大利润[blog.csdn.net](https://blog.csdn.net/llyzznn/article/details/123242410#:~:text=本文系统介绍了 动态规划 算法 的核心思想 和,0%2F1]表示第i天最多k次交易时的最大利润），并详细分析了不同变体的状态转移方程 和 代码实现，包括交易次数限制（1次、2次、k次）、无限制交易以及含冷冻期的情况。通过空间优化技巧，这些解法都能在O(n)或O(nk)时间内高效解决问题。)。这里的“交易”指一次买入+卖出。第三维用0表示不持有（空仓），1表示持有股票。在这个模型下，不同题目的差异通过限制k值或添加额外条件来体现：

- **交易次数限制**：如只能交易一次(k=1)、两次(k=2)[blog.csdn.net](https://blog.csdn.net/llyzznn/article/details/123242410#:~:text=本文系统介绍了 动态规划 算法 的核心思想 和,0%2F1]表示第i天最多k次交易时的最大利润），并详细分析了不同变体的状态转移方程 和 代码实现，包括交易次数限制（1次、2次、k次）、无限制交易以及含冷冻期的情况。通过空间优化技巧，这些解法都能在O(n)或O(nk)时间内高效解决问题。)或k次（一般k给定，小于天数长度）。如果k大于等于⌊n/2⌋则可视为无限次。
- **无限次交易**：可以理解为k很大，或者直接用无限次模型优化，当天交易次数不限。
- **冷冻期**：卖出后下一天无法买入，这会影响状态转移，需要在买入状态转移时考虑前两天的状态。
- **手续费**：每次卖出要扣费，影响卖出时的利润计算。

我们也可以不用三维数组，而是针对具体问题进行状态压缩，利用几个变量表示不同阶段的利润。例如，对于交易次数最多为2（LeetCode 123）的情形，可以用4个变量表示4种状态：第一次买入(buy1)、第一次卖出(sell1)、第二次买入(buy2)、第二次卖出(sell2)。初始时利润`sell1 = sell2 = 0`（还未卖出收益为0），`buy1 = buy2 = -∞`（还未买入视为利润极小）。随后遍历每一天的价格，不断更新这四种状态的最大利润[blog.csdn.net](https://blog.csdn.net/llyzznn/article/details/123242410#:~:text=本文系统介绍了 动态规划 算法 的核心思想 和,0%2F1]表示第i天最多k次交易时的最大利润），并详细分析了不同变体的状态转移方程 和 代码实现，包括交易次数限制（1次、2次、k次）、无限制交易以及含冷冻期的情况。通过空间优化技巧，这些解法都能在O(n)或O(nk)时间内高效解决问题。)。这种方法其实是上述dp[i][k][0/1]模型在k=2情形的展开，更直观。

**转移方程**：以**统一状态定义**的dp[i][k][0/1]模型来说，转移可分为几种情况：

- **不持有股票状态(0)\**可以来自\**前一天不持有**（延续空仓状态），或**前一天持有今天卖出**。
- **持有股票状态(1)\**可以来自\**前一天持有**（继续持有不操作），或**前一天不持有今天买入**。
   具体公式如下：

$$
\begin{aligned}
dp[i][k][0] &= \max(dp[i-1][k][0],\;\; dp[i-1][k][1] + \text{price}_i) \\ 
dp[i][k][1] &= \max(dp[i-1][k][1],\;\; dp[i-1][k-1][0] - \text{price}_i) 
\end{aligned}
$$

第一行：今天不持有 = max(昨天不持有，昨天持有今天卖出得到price收益)；
 第二行：今天持有 = max(昨天持有继续拿着，昨天不持有且交易次数少一买入花费price)。

边界条件：`dp[0][k][0] = 0`（第0天不持有收益0），`dp[0][k][1] = -price_0`（第0天持有即买入，收益为负花费）。当k=0时无交易许可应始终为0（不持有）或负无穷（持有状态不合法）。

不同变体在这个框架下稍作修改即可：

- 无限次交易相当于k非常大，可把`dp[i-1][k-1][0]`看作`dp[i-1][k][0]`（因为k不限，买入不消耗次数限制）。
- 含冷冻期（LeetCode 309）的模型需要在买入时参考前两天状态：购买时不能直接用`dp[i-1]`，而要用`dp[i-2][0] - price`以实现卖出后冷冻一天。
- 含手续费（LeetCode 714）在卖出时减去手续费，或等价地在买入成本中加上手续费。

**优化策略**：股票问题的DP一般时间O(n*k)，空间O(n*k)；但可以**滚动优化**到O(k)空间，因为第i天状态只与第i-1天相关。同时很多题k固定很小（1,2,或无穷），可以完全展开用常数个变量表示，如k=1用`buy1, sell1`两个变量就可, k=2用四个变量等，优化到O(n)时间和O(1)空间[blog.csdn.net](https://blog.csdn.net/llyzznn/article/details/123242410#:~:text=本文系统介绍了 动态规划 算法 的核心思想 和,0%2F1]表示第i天最多k次交易时的最大利润），并详细分析了不同变体的状态转移方程 和 代码实现，包括交易次数限制（1次、2次、k次）、无限制交易以及含冷冻期的情况。通过空间优化技巧，这些解法都能在O(n)或O(nk)时间内高效解决问题。)。这些优化基于状态转移的无后效性和重复结构，非常实用。

**典型问题**：股票问题在LeetCode有一个系列：

- LeetCode 121. **买卖股票的最佳时机** – 只能进行一次交易，求最大利润（k=1简化，贪心也可但DP思路清晰）。
- LeetCode 122. **买卖股票的最佳时机 II** – 不限交易次数，求最大利润（实际上等价于累加所有上升段利润，DP解法可得同样结果）。
- LeetCode 123. **买卖股票的最佳时机 III** – 最多2次交易，典型高级DP，需要考虑两个买卖时机。
- LeetCode 188. **买卖股票的最佳时机 IV** – 最多k次交易，一般k小于n，考验对状态的理解和优化。
- LeetCode 309. **最佳买卖股票时机含冷冻期** – 卖出后有一天不能交易。
- LeetCode 714. **买卖股票的最佳时机含手续费** – 每次交易支付手续费。

**示例代码：LeetCode 123 买卖股票的最佳时机 III**（最多进行2次交易）

```
int maxProfit(vector<int>& prices) {
    // 定义四个状态变量，初始化为极小值（buy*为负无限表示还未进行该买入）
    int buy1 = INT_MIN, sell1 = 0;
    int buy2 = INT_MIN, sell2 = 0;
    for (int price : prices) {
        // 更新第一次买入状态: 尽可能以最低价格买入（利润 = -price），取之前较大的（因为buy1是负数，数值大表示亏损少）
        buy1 = max(buy1, -price);
        // 更新第一次卖出状态: 尽可能以最高价卖出（利润 = 上次买入利润 + price）
        sell1 = max(sell1, buy1 + price);
        // 更新第二次买入状态: 相当于用第一次卖出的利润再买一次，利润 = sell1 - price
        buy2 = max(buy2, sell1 - price);
        // 更新第二次卖出状态: 利润 = 第二次买入利润 + 当前价卖出
        sell2 = max(sell2, buy2 + price);
    }
    return sell2;
}
```

上述代码用贪心更新形式实现了DP过程，每天更新所有状态。`buy1`表示在第i天前后进行第一次买入的最大利润（负值表示净支出），`sell1`是完成第一次卖出的最大利润，同理`buy2`和`sell2`对应第二次交易。【注意】第一次买入用`-price`初始化，第一次卖出初始为0（还未卖出收益为0），第二次买入初始也用`-price0`逻辑上相当于还没完成第一次卖就想进行第二次买——但由于只有当第一次卖出发生后`sell1`才会大于0，这个公式自然会在正确的时机起作用。**更严谨的方式**是将`buy2`初始化为非常大的负值（如`INT_MIN/2`），表示在无法进行第二次买的状态下利润极低，不过由于代码每轮取max，这里直接用相同初始化也能正确工作。该算法的时间复杂度O(n)，空间O(1)。类似地，可以很容易调整代码处理其他股票题（增加状态、添加冷冻期条件等）。

### 5.子串与子序列类动态规划

**特点与识别**：此类问题聚焦在字符串或序列的子串/子序列特性上。**子串**指原字符串中**连续**的一段，而**子序列**指可以**不连续**但保持相对顺序的序列。动态规划在处理字符串匹配、子序列比较方面极为常用。例如：最长公共子序列(LCS)、最长公共子串、编辑距离、字符串交织、正则匹配、回文子串/子序列等。这类题的输入通常是一个或两个字符串/序列，问题问的是某种**匹配**、**变换**或**优化选择**，例如“两个字符串的最长公共子序列长度”，“一个字符串变成另一个的最少操作数”等。识别要点在于：**子序列**问题往往需要比较序列的不同位置，尝试跳过某些元素配对匹配，会有两个索引的状态；**子串**问题通常可以转化为类似区间DP或双指针，但有时也能用一维DP加滚动检测。

**状态定义**：通常我们用二维状态来处理两个序列/字符串的子序列匹配问题：`dp[i][j]`表示**序列A前i个元素**和**序列B前j个元素**所得到的某种结果。例如：

- 对于**最长公共子序列**，`dp[i][j]`定义为A的前i个字符和B的前j个字符的LCS长度[cloud.tencent.com](https://cloud.tencent.com/developer/article/2437715#:~:text=例子2：最长公共子序列)。
- 对于**编辑距离**，`dp[i][j]`定义为将A前i个字符编辑成B前j个字符所需的最少操作数。
- 对于**匹配类**（如正则匹配，通配符匹配），`dp[i][j]`通常表示A前i和B前j能否匹配（布尔值）。

对于单个字符串的问题（如回文子串、回文子序列、子串比较），可以降为一维或二维区间状态：例如回文子串通常用`dp[i][j]`表示子串[i..j]是否为回文，然后在双重循环里填表；最长回文子串长度则在判断回文时更新最大长度。**最长回文子序列**前面已经示范过，它本质也是单个序列的区间DP。**最长上升子序列**(LIS)也属于子序列类，只是相比LCS是一维序列自身的子序列问题，用一维DP解决。

**转移方程**：以**最长公共子序列(LCS)**为例，如果我们用`dp[i][j]`表示`text1[0..i-1]`和`text2[0..j-1]`的LCS长度，则有经典转移：

- 如果第i个和第j个字符相等（即 `text1[i-1] == text2[j-1]`），那么这个公共字符可以增加子序列长度：
   `dp[i][j] = dp[i-1][j-1] + 1`。
- 否则，不相等时就看哪边丢弃一个字符带来的子序列更长：
   `dp[i][j] = max(dp[i-1][j], dp[i][j-1])`。

边界条件是`dp[0][j] = 0`和`dp[i][0] = 0`（任何一个序列长度为0时，公共子序列长度为0）。

其他问题类似：

- 编辑距离转移有删除、插入、替换三种操作，所以`dp[i][j]`取自`dp[i-1][j]+1`（删）、`dp[i][j-1]+1`（插）、`dp[i-1][j-1] + (text1[i-1]==text2[j-1]?0:1)`（替换或无操作）三者最小。
- 不同的子序列计数（LeetCode 115）用长整型计数，转移分字符相等或不等分别累加。
- 回文子串判断用区间dp或双指针，转移为`dp[l][r] = (s[l]==s[r]) AND dp[l+1][r-1]`，需注意区间长度<=2作为特判。

**优化策略**：双序列DP如LCS、编辑距离的空间可优化到O(min(m,n))，因为每次计算第i行时只需要第i-1行的数据[cloud.tencent.com](https://cloud.tencent.com/developer/article/2437715#:~:text=1)。实现上可以使用两个数组滚动更新，或甚至一个数组从后往前更新（以避免覆盖尚未用到的数据）实现空间压缩。这样空间复杂度从O(m*n)降为O(n)。对于一些特殊子序列问题也有进一步优化算法（比如编辑距离有优化但较复杂）。**子串**类问题如果只需要长度结果，也有优化算法如后缀数组/后缀自动机等，但这些一般超出面试范围，掌握DP解法已足够。

**典型问题**：

- LeetCode 1143. **最长公共子序列 (LCS)** – 经典双串DP。

  ```C++
  // 目标：计算 text1 和 text2 字符串的最长公共子序列
  // 状态：字符串  text1 的位置 i 和字符串 text2 的位置 j
  // dp函数定义：dp[i][j] 表示`text1[0..i-1]`和`text2[0..j-1]`的LCS长度
  // 选择：
  	// 如果第i个和第j个字符相等，那么这个公共字符可以增加子序列长度
  	// 否则，不相等时就看哪边丢弃一个字符带来的子序列更长
  // 状态转移方程：
  	// dp[i][j] = dp[i-1][j-1] + 1
  	// dp[i][j] = max(dp[i-1][j], dp[i][j-1])
  // base case :
  	// dp[i][0] = 0; dp[0][j] = 0;
  class Solution {
  public:
      int longestCommonSubsequence(string text1, string text2) {
          int m = text1.size();
          int n = text2.size();
          
          // base case
          vector<vector<int>>dp(m+1, vector<int>(n+1, 0));
          
          for (int i = 1; i <= m; i++) {
              for (int j = 1; j <= n; j++) {
                  if (text1[i-1] == text2[j-1]) {
                      dp[i][j] = dp[i-1][j-1] + 1;
                  } else {
                      dp[i][j] = max(dp[i-1][j], dp[i][j-1]);
                  }
              }
          }
          
          return dp[m][n];
      }
  };
  ```

  

- LeetCode 72. **编辑距离** – 计算两个字符串的最小编辑操作数。

  ```C++
  // 目标：获得字符串 word1 转换成字符串 word2 的最少次数
  // 状态：两字符串的位置 i, j
  // dp[i][j] 表示: word1 前 i 个字符（下标 0..i-1）替换成 word2 前 j 个字符（下标 0..j-1）的最少操作次数
  // 选择：
  	// 当前位置,两字符相同，次数保持不变
  	// 当前位置，两字符不相同，次数加一
  	// 当前位置，有一个字符为空
  // 状态转移方程：考虑 word1[i-1] 与 word2[j-1]
  	// 若相等：尾字符已对齐，不用操作：
  		//dp[i][j] = dp[i-1][j-1]
  	// 若不相等：三选一取最小（每个代价 +1）
  		// 替换 word1[i-1] → word2[j-1]：
  			// dp[i][j] = dp[i-1][j-1] + 1
  		// 删除 word1[i-1]（让 word1[0..i-2] 匹配 word2[0..j-1]）：
  			// dp[i][j] = dp[i-1][j] + 1
  		// 插入 在 word1 尾部插入 word2[j-1]（让 word1[0..i-1] 匹配 		word2[0..j-2]使其匹配 word2[j-1]）：
  			// dp[i][j] = dp[i][j-1] + 1
  // base case:
  	// dp[0][j] = j：空串变成 word2 的前 j 个字符，需要 j 次插入。
  	// dp[i][0] = i：word1 的前 i 个字符变成空串，需要 i 次删除。
  // 返回：dp[m][n];
  class Solution {
  public:
      int minDistance(string word1, string word2) {
          int m = word1.size();
          int n = word2.size();
          
          vector<vector<int>> dp(m+1, vector<int>(n+1, 0));
          // base case
          for (int i = 0; i <= m; i++) {
              for (int j = 0; j <= n; j++) {
                  if (i == 0) dp[i][j] = j; // 插入 j 个字符
                  if (j == 0) dp[i][j] = i; // 删除 i 个字符
              }
          }
          
          for (int i = 1; i <= m; i++) {
              for (int j = 1; j <= n; j++) {
                  if (word1[i-1] == word2[j-1]) {
                      dp[i][j] = dp[i-1][j-1];
                  } else {
                      dp[i][j] = min({
                          			dp[i-1][j-1] + 1, // 替换
                          			dp[i-1][j] + 1,   // 删除
                          			dp[i][j-1] + 1	  // 插入
                      });
                  }
              }
          }
          return dp[m][n];
      }
  };
  ```

  

- LeetCode 115. **不同的子序列** – 计算一个字符串作为另一个字符串子序列的不同匹配个数。

  ```C++
  // 目标：获得字符串 s 的子序列中出现字符串 t 的次数
  // 状态：索引 i , j
  // dp[i][j]定义：用 s 的前 i 个字符（s[0..i-1]）组成 t 的前 j 个字符（t[0..j-1]）的不同子序列个数。
  // 选择：
  	// 两字符相等：
  		// 匹配这两个字符
  		// 不匹配这两个字符
  	// 两字符不相等；
  		// 只能丢弃 s 中的字符
  // 状态转移方程：
  	// 若 s[i-1] == t[j-1]:
  		// 匹配这两个字符：从 dp[i-1][j-1] 继承:
  			// （把 s[i-1] 和 t[j-1] 配对，剩下看 s[0..i-2] 和 t[0..j-2]）
  		// 不匹配（丢弃 s[i-1]）：从 dp[i-1][j] 继承
  			// （相当于不用 s[i-1]，还是要在 s[0..i-2] 中找 t[0..j-1]）
  		// 综合：
  			// dp[i][j] = dp[i-1][j-1] + dp[i-1][j]
  	// 若 s[i-1] != t[j-1]:
  		// （丢弃 s[i-1]，用前 i-1 个字符去匹配整个 t[0..j-1]）
  		// 只能丢弃 s[i-1]：dp[i][j] = dp[i-1][j]
  // base case:
  	// dp[i][0] = 1:空串 "" 是任意字符串的子序列，且只有 1 种取法（全部删掉）。
  	// dp[0][j] = 0:(j>0)：空的 s 无法组成非空的 t。
  // 返回值：
  	// dp[m][n]
  class Solution {
  public:
      int numDistinct(string s, string t) {
          int m = int(s.size());
          int n = int(t.size());
          
          vector<vector<int>> dp(m+1, vector<int>(n+1, 0));
          
          // base case
          for (int i = 0; i <= m; i++) dp[i][0] = 1;
          
          for (int i = 1; i <= m; i++) {
              for (int j = 1; j <= n; j++) {
                  if (s[i-1] == t[j-1]) {
                      dp[i][j] = dp[i-1][j-1] // 直接匹配
                          	 + dp[i-1][j];	// 不匹配
                  } else {
                      dp[i][j] = dp[i-1][j];
                  }
              }
          }
          
          return dp[m][n];
      }
  };
  ```

  

- LeetCode 5. **最长回文子串** – 可用DP（O(n^2)）或中心扩展(O(n^2))解决。DP需记录子串是否回文。

  ```c++
  // 目标：找到字符串 s 的最长回文子串
  // 状态：i, j 表示字符串 s[i..j]
  // dp[i][j]表示：表示子串 s[i..j]（含两端）是否为回文串。
  // 选择：
  	// 考虑两端字符 s[i] 与 s[j]：
  		// 
  class Solution {
  public:
      string longestPalindrome(string s) {
          int n = (int)s.size();
          if (n <= 1) return s;
  
          // dp[i][j]：s[i..j] 是否回文
          vector<vector<bool>> dp(n, vector<bool>(n, false));
  
          int start = 0;    // 最长回文的起点
          int maxLen = 1;   // 最长回文的长度（至少 1）
  
          // 1) 单字符都是回文
          for (int i = 0; i < n; ++i) dp[i][i] = true;
  
          // 2) 按“区间长度 L”从小到大枚举（L >= 2）
          for (int L = 2; L <= n; ++L) {
              for (int i = 0; i + L - 1 < n; ++i) {
                  int j = i + L - 1;  // 区间右端
  
                  if (s[i] != s[j]) {
                      dp[i][j] = false;
                  } else {
                      // s[i] == s[j]
                      if (j - i <= 1) {
                          // 长度为 2（"aa"）或 1（已在上面处理），这里覆盖 L==2 的情况
                          dp[i][j] = true;
                      } else {
                          // 长度 >= 3，取决于内部是否回文
                          dp[i][j] = dp[i + 1][j - 1];
                      }
                  }
  
                  // 更新最长答案
                  if (dp[i][j] && L > maxLen) {
                      start = i;
                      maxLen = L;
                  }
              }
          }
  
          return s.substr(start, maxLen);
      }
  };
  ```

  

- LeetCode 10. **正则表达式匹配** – 带有通配符`.`和`*`的匹配，典型DP解法。

- LeetCode 44. **通配符匹配** – `?`和`*`的简单通配符匹配，DP或贪心。

- LeetCode 1035. **不相交的线** – 实际就是LCS问题的一个表述（求两个数组的最长公共子序列）。

- LeetCode 300. **最长上升子序列** – 子序列类，但输入是一个序列自己，转移须从前面的状态获取最大+1。

### 6.博弈型动态规划

**特点与识别**：博弈类问题指的是两名玩家轮流行动、相互博弈的情形，通常假设双方都**采取最优策略**。常见例子有石头取子游戏、纸牌游戏、棋类局部状态等。题目往往问“先手是否会赢”或“最优得分差是多少”等。在这类问题中，由于双方对立，我们需要考虑**极小化极大**的策略，即一方最大化自己的收益，另一方最小化对方的收益。**识别**：如果题目描述两个玩家轮流从一组元素（数组、石头堆等）中取走东西，问先手能否赢/最大得分，基本就是博弈型DP。通常输入是一个数组或区间，每次决策从中取某些元素（常为两端或固定数量），双方最优行为导致一个博弈过程。

**状态定义**：常用**区间DP**来解决。定义`dp[i][j]`表示在当前玩家面对区间`[i, j]`的一组石头（或数字）时，**该玩家相对于另一玩家能够获得的最大净胜分**（即自己的得分减去对方得分）。这个定义有点巧妙：如果我们能算出这个差值≥0，说明当前玩家不输给对方；如果为正则当前玩家有领先优势。【注】也有其他定义方式，比如定义dp为先手的绝对最大得分，但需要同时计算对手得分或总和来比较，不如净胜分法简洁。

以石头游戏为例（两端取石子），当轮到当前玩家时：

- 如果取左端石子`nums[i]`，那么对手随后得到的净胜分是`dp[i+1][j]`（在区间[i+1,j]对手成为“当前玩家”）。但因为角色切换，这个净胜分实际上是**对手领先**的分差。所以当前玩家自己的净胜分会是：取左端后的自己的得分`nums[i]`减去对手随后净胜分，即 `nums[i] - dp[i+1][j]`。
- 如果取右端石子`nums[j]`，同理净胜分为：`nums[j] - dp[i][j-1]`（拿右端后，对手面对区间[i,j-1]，得到差值dp[i][j-1]）。

当前玩家会选择让自己净胜分最大的那个决策，因此：
 $dp[i][j] = \max(\, nums[i] - dp[i+1][j],\;\; nums[j] - dp[i][j-1] \,) .$
 边界情况下，如果区间长度为1（i==j），则`dp[i][i] = nums[i]`，因为只有当前玩家拿这一堆石子的选择，没有对手得分。

**转移方程**：正如上面推导，对于**先手最大化，后手最小化**的问题，通过定义dp为当前玩家对另一个玩家的净胜分，我们巧妙地将两人决策合二为一地体现在一个公式中：当前玩家选择一端，然后相减对手的结果。这是常见的博弈DP转移方式。对于其他博弈规则（比如每次可以拿1到3根火柴等），转移要基于当前可以采取的动作枚举。如果可以拿k个不同的元素，则要检查每个动作导致的对手结果并取最优/最劣。一般来说，**博弈问题的DP状态要反映轮到谁、剩余资源的情况**，常常和区间DP或背包DP结合（如拿走固定数字的问题可以用背包思维）。

**优化策略**：博弈类DP通常是O(n^2)（区间两端游戏）或基于总数量N的O(N*M)等。由于状态空间往往是局面数，对于两个玩家的完美决策，很少有比DP更简单的高效解法（除非有数学性质可直接判断）。但是一些游戏通过**数学分析**可以推导胜负规律（如Nim博弈通过异或和判断胜负）。面试中，如果题目像石头游戏这样**输入规模不大 (<= 500)**，可以直接DP求解；如果输入可能很大，那通常游戏有特殊性质（如**必胜态**判定等）可利用贪心或公式。总之，要根据题目具体情形判断采用通用DP还是特殊技巧。

**典型问题**：

- LeetCode 486. **预测赢家** – 给定数组，双方从两端取数，判断先手是否能赢（经典区间博弈问题）。
- LeetCode 877. **石子游戏** – 和预测赢家类似，不过数组长度是偶数条件下先手总能赢（这题结论先手必胜，但通用解法同486类似）。
- LeetCode 464. **我能赢吗** – 从1到20选择数字累加到目标，总结先手是否有必胜策略（状态压缩+记忆化DFS，不属于简单DP表格）。
- LeetCode 1140. **石子游戏 II** – 石子游戏变体，每次可以拿前面2M堆石子，带来额外维度，需要DP[i][M]来表示状态。
- 经典**Nim游戏** – 一堆石子拿走任意颗，异或和判胜负（数学归纳结论）。
- 其他博弈题往往有自成一派的解法，但底层思想依然是**递归+记忆**或**DP**考虑所有可能行动的结果。

**示例代码：LeetCode 486 预测赢家**（两人从数组两端拿数，先手能否赢）

```
bool PredictTheWinner(vector<int>& nums) {
    int n = nums.size();
    // dp[i][j] 表示在数组子区间 [i..j] 上，当前玩家对另一个玩家的最大净胜分
    vector<vector<int>> dp(n, vector<int>(n, 0));
    // 当区间长度为1时，当前玩家只能拿这一个数
    for (int i = 0; i < n; ++i) {
        dp[i][i] = nums[i];
    }
    // 枚举区间长度，从2到n
    for (int len = 2; len <= n; ++len) {
        for (int i = 0; i + len - 1 < n; ++i) {
            int j = i + len - 1;
            // 当前玩家选择左端或右端
            int takeLeft = nums[i] - dp[i+1][j];   // 拿左边，然后净胜分 = 该值 - 对手在剩余[i+1..j]的净胜分
            int takeRight = nums[j] - dp[i][j-1];  // 拿右边，然后净胜分 = 该值 - 对手在剩余[i..j-1]的净胜分
            dp[i][j] = max(takeLeft, takeRight);
        }
    }
    // 如果最终先手在面对整个区间的净胜分>=0，说明先手不输（赢或平）
    return dp[0][n-1] >= 0;
}
```

这段代码计算了先手在整个数组范围[0..n-1]上的净胜分差。如果结果非负，则先手得分至少不低于对手（赢或平局算先手胜）。转移里，若拿左端，则自己得`nums[i]`，对手随后得到`dp[i+1][j]`的差值，这个差值是对手领先多少，所以要减掉；拿右端同理。这样一来，将双方博弈转化为了单人的差值最大化问题。这个解法的复杂度为O(n^2)，足以应对常规模试输入规模。

### 7.其他类型动态规划

上述分类已经覆盖了面试中绝大多数动态规划问题。然而动态规划的应用非常广泛，还有一些特殊类别值得简要提及：

- **树形DP**：在树（或图）结构上进行DP，根据子节点状态来推导父节点状态。例如二叉树的路径和、树的直径、选点染色问题等都用树形DP。状态一般定义在节点上，考虑选/不选节点或不同子树合并。树形DP需要自底向上遍历树（后序遍历计算子树信息）。
- **状态压缩DP**：用一个整数的**二进制位表示子集选择状态**的DP，通常用于集合划分、旅行商问题(TSP)等。状态通常是`dp[mask][j]`表示当前子集mask以及某个当前位置j的最优值。由于2^N状态爆炸，仅适用于N较小(一般N<=20左右)的问题。
- **数字DP（数位DP）**：针对**整数的每一位**进行DP，统计满足特定性质的数字个数。状态按“最高位到当前位的构成”和一些标志（如前缀是否已达到上界）来定义。常用于计数某范围内数字满足限制（如不含某数字的个数等）。数位DP模板固定，但比较进阶，这里不展开。
- **概率DP**：DP 状态表示某种概率，转移时累加概率。例如博弈胜率、随机过程等问题，有时用DP叠加概率值。
- **多维DP**：有的复杂问题需要多个维度状态，比如上面股票就是三维（天数×交易次数×持有与否），再如有些背包是二维容量或者额外参数，都会增加DP维度。维度越多时间复杂度越高，但必要时也只能如此建模。
- **混合型**：有些问题结合了多个类别，比如区间博弈（既是区间DP又是博弈）、带约束的路径（序列DP和背包杂糅）等。面对复杂问题时，**关键是根据题意刻画出合适的状态**，不必拘泥于某种分类，而是灵活运用DP的思想来分解问题。

## 3.总结

动态规划问题在面试中占有重要地位，题目种类繁多，但万变不离其宗：找**状态**，列**转移**，重用**子问题结果**。通过上述分类归纳，我们可以提炼出一些**通用解题框架**：

- **识别DP场景**：一看问题要求的目标（最优值/计数/可行性），二看是否存在重叠子结构和最优子结构。如果决策过程需要尝试不同选项组合全局最优，往往就是DP[blog.csdn.net](https://blog.csdn.net/llyzznn/article/details/123242410#:~:text=4)。面试中拿到题先想：能否定义子问题？是否有递推关系？
- **定义状态**：没有统一的格式，但常用的就那几种：按序列索引定义dp[i]；按两序列索引dp[i][j]；按区间dp[i][j]；按背包容量dp[i][cap]；按组合状态dp[mask]等。写下状态的物理含义，明确它包含的变量和范围。
- **列转移方程**：根据最后一步或者一次决策结果来写转移是可靠的方法。问自己：“如何由更小的子问题得到当前问题？” 通常会有几种选择，把每种选择对状态的影响考虑清楚并取最优（或累加计数）。这一过程可以先用递归描述，再转化为循环实现。转移方程的推导要保证**覆盖所有可能策略**且**不重复**，这需要多加练习不同题型总结经验。
- **初始和边界**：给出最小规模问题的答案，例如dp[0]=... 或dp[i][i]=...等，这些是递推的起点。别忘了处理一些特殊情况如空输入、容量为0等等。
- **实现与优化**：选择自顶向下（递归+memo）还是自底向上（迭代）实现。一般面试推荐迭代，因为容易把握不会爆递归栈。实现中注意循环顺序、数组大小等细节。能优化空间的尽量优化，既展示能力又避免大数组。
- **举例验证**：小例子走一遍DP表推演，检查转移是否正确。面试时可以通过举例来向面试官说明你的状态转移过程，加深对方对你解法的理解和认可。

动态规划的熟练度来自于**多做题、多总结**。初学者常觉得DP难，其实是陌生的问题模型没见过，不知道怎么定义状态和转移。通过分类学习，如背包问题就是“选或不选”的套路、股票问题就是“状态机”的套路、子序列问题就是“双指针比较”的套路，逐渐地遇到新题目也能**快速类比**到之前做过的类似题型，自然就能想到用DP并写出解法框架。希望以上总结能帮助您在面试中迅速识别DP问题，并独立构造出解决方案。只要不断练习、熟悉这些套路，动态规划将不再令您畏惧，而会成为解题利器。祝好运！[cloud.tencent.com](https://cloud.tencent.com/developer/article/2437715#:~:text=)[blog.csdn.net](https://blog.csdn.net/llyzznn/article/details/123242410#:~:text=4)

在写动态规划题时，很多同学卡住的原因就是**不知道先想什么**。我给你一个**标准的分析顺序**，以后你可以按这个套路走：

------

### 1️⃣ 明确问题目标

先问自己：**题目让我算什么？**

- 最大/最小值？
- 方案数？
- true/false 判断？

👉 这一步决定了你最后 `dp(...)` 的返回值是什么。

------

### 2️⃣ 定义状态（state）

**状态就是子问题的描述方式。**
 想清楚：如果原问题很大，你把它拆成更小的子问题，要用哪些变量来刻画？

例子：

- 爬楼梯：子问题就是“爬到第  i  阶的方法数”，状态就是楼梯台阶数  i 。
- LIS：子问题就是“以 nums[i] 结尾的 LIS 长度”，状态就是位置  i 。
- 背包：子问题就是“前  i  件物品放入容量  j  的背包”，状态是 (i, j)。
- 股票：子问题是“第  i  天，交易次数  k ，是否持股”，状态是 (i, k, 持股与否)。

👉 **先有状态，才谈得上转移**。

------

### 3️⃣ 定义 dp 数组 / 函数

一旦状态变量确定，就要写下来：

- **dp 数组的含义**（二维/三维数组常见），或者
- **dp 函数的含义**（记忆化递归）。

> 这一步要用一句话描述清楚：“dp[i][j] 表示……”，“dp(i, j) 返回……”。

👉 这一步非常关键，如果定义模糊，后面都会乱。

------

### 4️⃣ 明确选择（choice）

状态是“我在哪”，选择是“我能怎么走”。
 想清楚：在这个状态下，有哪些可能的决策？

例子：

- LIS：以 `nums[i]` 结尾，可以选择接在某个 `j<i && nums[j]<nums[i]` 后面。
- 背包：第 i 件物品，可以选（装进背包），或不选。
- 股票：今天可以买、卖、休息。
- 爬楼梯：可以爬 1 阶，或 2 阶。

👉 **选择是状态转移的根源**。

------

### 5️⃣ 写出状态转移方程

把选择翻译成公式。

- LIS: `dp[i] = max(dp[j] + 1)` for all `j<i && nums[j]<nums[i]`
- 背包: `dp[i][j] = max(dp[i-1][j], dp[i-1][j-w] + v)`
- 爬楼梯: `dp[i] = dp[i-1] + dp[i-2]`

👉 转移方程 = 状态 + 选择的结合。

------

### 6️⃣ 初始条件（base case）

看最小子问题：dp 的起点是多少？

- LIS: 每个位置至少长度 1。
- 爬楼梯: `dp[0]=1`，`dp[1]=1`。
- 背包: 容量为 0 → 价值 0；没有物品 → 价值 0。

👉 base case 其实就是最小状态的答案。

------

### 7️⃣ 决定计算顺序

- **自顶向下**：递归 + 备忘录。写法直观，容易想清楚转移。
- **自底向上**：填表。常见做法，按状态依赖顺序写循环。

👉 顺序必须保证：计算一个状态时，它依赖的子状态已经算好。

------

### 顺序总结口诀

**目标 → 状态 → dp 定义 → 选择 → 转移方程 → base case → 计算顺序**

------

### 
