# 一、14天推荐与搜索算法面试学习计划

**前提：** 学习者是应届生，具备PyTorch、常见神经网络（CNN、RNN）和Transformer基础，每天可投入4-5小时学习。本计划涵盖推荐系统与搜索算法岗位所需的理论和实践，循序渐进地从推荐模型过渡到搜索模型，并融合两者方法。第13-14天安排综合复盘与模拟面试准备。



## Day 1: 推荐系统基础与CTR预估概念

*推荐系统流程架构：底层数据源与特征平台，上层召回及排序层次[woshipm.com](https://www.woshipm.com/pd/4223123.html#:~:text=,排序层：利用机器学习或者深度学习模型，以及更丰富的特征进行重排序，筛选出更小、更精准的推荐集合返回给上层业务。)。*

- **理论学习目标：** 理解推荐系统的整体架构和工作流程，明确推荐系统要解决的问题、核心环节（召回与排序）和关键指标。了解CTR（Click-Through Rate）预估在推荐/广告中的意义，以及推荐与搜索的区别。
- **重点模型/概念：** 推荐系统三阶段：**召回**（从海量物料中筛选候选集）、**排序**（精排候选集）和**重排序**（多目标调整）[woshipm.com](https://www.woshipm.com/pd/4223123.html#:~:text=推荐系统本质上就是一个信息过滤系统，通常分为：召回、排序、重排序这3个环节，每个环节逐层过滤，最终从海量的物料库中筛选出几十个用户可能感兴趣的物品推荐给用户。)[woshipm.com](https://www.woshipm.com/pd/4223123.html#:~:text=,排序层：利用机器学习或者深度学习模型，以及更丰富的特征进行重排序，筛选出更小、更精准的推荐集合返回给上层业务。)；CTR定义及其在广告排序中的作用；**评价指标**：CTR任务常用对数损失(logloss)和AUC衡量模型预测效果fancyerii.github.io。推荐 vs 搜索：搜索有明确查询意图，结果与查询直接相关；推荐无明确查询，依据用户历史偏好进行个性化推荐[woshipm.com](https://www.woshipm.com/pd/4223123.html#:~:text=,广告：借助搜索和推荐技术实现广告的精准投放，可以将广告理解成搜索推荐的一种应用场景，技术方案更复杂，涉及到智能预算控制、广告竞价等。)。
- **推荐资料：**
  - 博客：「CTR预估模型综述」[cnblogs.com](https://www.cnblogs.com/zhengzhicong/p/12862816.html#:~:text=一、传统CTR预估模型演变)[cnblogs.com](https://www.cnblogs.com/zhengzhicong/p/12862816.html#:~:text=相比于POLY2，FM为每个特征学习了一个隐权重向量 w。在特征交叉时，使用两个特征隐向量w的内积作为交叉特征的权重。)（介绍推荐系统发展和CTR基础）。
  - 视频：B站《推荐系统从入门到实战》系列[bilibili.com](https://www.bilibili.com/video/BV1Dz411B7wd/#:~:text=推荐系统从入门到实战 推荐系统从入门到实战共计10条视频，包括：为什么要学习推荐系统、推荐系统包含哪些环节、推荐系统有哪些召回路径等，UP主更多精彩视频，请关注UP账号。)（10集，涵盖推荐系统环节、召回和排序等）。
  - 博客：「23张图带你入门推荐系统」[woshipm.com](https://www.woshipm.com/pd/4223123.html#:~:text=,排序层：利用机器学习或者深度学习模型，以及更丰富的特征进行重排序，筛选出更小、更精准的推荐集合返回给上层业务。)[woshipm.com](https://www.woshipm.com/pd/4223123.html#:~:text=,排序层：利用机器学习或者深度学习模型，以及更丰富的特征进行重排序，筛选出更小、更精准的推荐集合返回给上层业务。)（图解推荐系统架构与流程）。
- **实战任务：** 以一个简单实例体验推荐系统流程。选取MovieLens小数据集，尝试按照“召回->排序”流程构建简易推荐：先用物品热门度或协同过滤产生候选电影列表，再用用户评分预测排序。计算并观察不同推荐结果的CTR或命中率。熟悉数据加载、基础特征处理和评价指标计算，为后续深入模型做准备。

## Day 2: CTR预估传统模型与特征工程

- **理论学习目标：** 掌握CTR预估的传统建模方法，理解特征交叉和高维稀疏特征问题。学习从逻辑回归（LR）到因子分解机（FM）等模型的演进，以及深度模型兴起前业界提升CTR的思路。了解在CTR预估中进行特征工程（如特征组合、字段概念）的方法。
- **重点模型/概念：** **逻辑回归（LR）**：线性模型，易于实现但只能学习各特征线性权重，无自动特征组合能力[cnblogs.com](https://www.cnblogs.com/zhengzhicong/p/12862816.html#:~:text=)。**多项式2阶模型（Poly2）**：将所有特征两两组合，解决LR无法特征交互的问题但带来维度爆炸[cnblogs.com](https://www.cnblogs.com/zhengzhicong/p/12862816.html#:~:text=POLY2对所有特征进行“暴力”组合（即两两交叉），并对所有的特征组合赋予了权重。)。**因子分解机（FM）**[cnblogs.com](https://www.cnblogs.com/zhengzhicong/p/12862816.html#:~:text=3)：用隐向量表示每个特征，内积模拟二阶特征交互，参数量从O(n^2)降到O(n·k)[cnblogs.com](https://www.cnblogs.com/zhengzhicong/p/12862816.html#:~:text=相比于POLY2，FM为每个特征学习了一个隐权重向量 w。在特征交叉时，使用两个特征隐向量w的内积作为交叉特征的权重。)。**Field-aware FM (FFM)**：在FM基础上引入字段概念，不同特征域采用不同隐向量，提升表达但复杂度更高[cnblogs.com](https://www.cnblogs.com/zhengzhicong/p/12862816.html#:~:text=4)。**GBDT+LR**：用梯度提升树自动做特征组合，提取叶子节点作为离散特征，再喂入LR模型，缓解人工特征工程难题[infoq.cn](https://www.infoq.cn/article/q8cv-4vq6jd8974w9mxj#:~:text=上篇文章介绍的 FFM 模型采用引入特征域的方式增强了模型的表达能力，但无论如何，FFM 只能够做二阶的特征交叉，如果要继续提高特征交叉的维度，不可避免的会发生 组合爆炸和计算复杂度过高的情况。那么有没有其他的方法可以有效的处理高维特征组合和筛选的问题？2014,年，Facebook 提出了基于 GBDT%2BLR 组合模型的解决方案。)[infoq.cn](https://www.infoq.cn/article/q8cv-4vq6jd8974w9mxj#:~:text=大家知道，GBDT 是由多棵回归树组成的树林，后一棵树利用前面树林的结果与真实结果的残差做为拟合目标。每棵树生成的过程是一棵标准的回归树生成过程，因此每个节点的 分裂是一个自然的特征选择的过程，而多层节点的结构自然进行了有效的特征组合，也就非常高效的解决了过去非常棘手的特征选择和特征组合的问题。)。这些模型体现了深度学习前通过**特征工程+浅层模型**提升CTR预估的思路。
- **推荐资料：**
  - 博客：「前深度学习时代CTR预估模型演化」[infoq.cn](https://www.infoq.cn/article/q8cv-4vq6jd8974w9mxj#:~:text=在上篇专栏文章《前深度学习时代CTR预估模型的演化之路——从LR到FFM》中，我们沿着特征交叉这条发展路线，回顾了从 LR 到 POLY2，FM，再到 FFM,PLM CTR 模型。)[infoq.cn](https://www.infoq.cn/article/q8cv-4vq6jd8974w9mxj#:~:text=GBDT%2BLR——特征工程模型化的开端)（介绍从LR、Poly2、FM到GBDT+LR等演进）。
  - 博客：「DeepFM模型CTR预估理论与实战」fancyerii.github.iofancyerii.github.io（含CTR任务定义、采样偏差、评价指标介绍）。
  - 教程：GitHub项目 **DeepCTR**fancyerii.github.io – 一个开源库实现多种CTR模型，可帮助理解和快速试验LR、FM等模型。
- **实战任务：** 使用公开CTR数据集（如**Kaggle Criteo广告点击**数据）进行实验。尝试训练一个基线LR模型进行点击率预测：
  1. **数据预处理：** 将类别特征编码、数值特征离散化，准备训练集和验证集。
  2. **训练LR模型：** 利用现有库（如scikit-learn或PyTorch）训练逻辑回归模型，计算logloss和AUC。
  3. **加入特征交叉：** 手工组合部分特征或使用Factorization Machines库训练FM模型，对比效果提升。
  4. **观察结果：** 分析LR与FM的性能差异，体会特征交互对CTR预估的作用。
     通过此实验，巩固特征工程要点，理解传统CTR模型的长短板，为引出深度CTR模型做好铺垫。

## Day 3: 深度CTR模型（Wide & Deep，DeepFM）理论与实践

- **理论学习目标：** 学习深度学习在CTR预估中的应用，理解将浅层模型与深层神经网络结合的动机和方法。重点掌握**Wide & Deep**和**DeepFM**模型结构，了解它们如何自动学习高阶特征交互。
- **重点模型/概念：** **Wide & Deep模型**：谷歌提出，将记忆式的“宽”部分（手工特征组合的LR）与泛化性的“深”部分（多层感知机DNN）相结合[cnblogs.com](https://www.cnblogs.com/zhengzhicong/p/12862816.html#:~:text=3)。Wide部分捕获稀疏特征组合，Deep部分捕获潜在高阶关系，二者输出加权和用于CTR预测。**DeepFM**：在Wide & Deep基础上，用FM替代Wide部分，提高模型自动学习特征组合的能力[cnblogs.com](https://www.cnblogs.com/zhengzhicong/p/12862816.html#:~:text=4)。DeepFM的FM分支负责二阶特征交互，DNN分支学习高阶非线性关系，无需人工特征组合[cnblogs.com](https://www.cnblogs.com/zhengzhicong/p/12862816.html#:~:text=4)。此外了解**Embedding层**在深度CTR模型中的作用：将高维稀疏ID特征映射为低维稠密向量，再输入DNN[cloud.tencent.com](https://cloud.tencent.com/developer/article/1795015#:~:text=文章首先介绍了现有的点击率 ,Layer Perceptron) 中， 从而学习这些特征间的非线性关系。)。深度CTR模型的优势在于**端到端**地学习特征表示和组合，降低特征工程成本。
- **推荐资料：**
  - 论文：Guo等人 *“DeepFM: A Factorization-Machine based Neural Network for CTR Prediction”*（2017）[cnblogs.com](https://www.cnblogs.com/zhengzhicong/p/12862816.html#:~:text=4)（DeepFM原论文，可了解模型架构细节）。
  - 博客：「DeepFM模型原理与实战」fancyerii.github.iofancyerii.github.io（通俗解释DeepFM的Wide部分和Deep部分设计，附PyTorch实现示例）。
  - 视频：YouTube《推荐系统公开课——排序模型》[youtube.com](https://www.youtube.com/watch?v=nT_74f3E7c4#:~:text=推荐系统公开课——8小时完整版，讲解工业界真实的推荐 ... )（讲解工业界排序模型，包括Wide&Deep、DeepFM的思路）。
- **实战任务：** 基于上一日的数据集，实践深度CTR模型：
  1. **使用开源框架试验DeepFM：** 利用DeepCTR库或RecStudio等工具，调用DeepFM模型在Criteo数据集上训练，观察训练过程logloss、AUC变化fancyerii.github.io。
  2. **分析模型结构：** 输出DeepFM模型摘要，确认Embedding层大小、FM交叉输出和DNN层结构，加深对模型各部分的理解。
  3. **对比效果：** 将DeepFM结果与Day2的LR/FM结果比对，通常DeepFM能取得更低logloss、更高AUC，说明深度网络确实帮助学习了更复杂的特征关系。
  4. **阅读代码实现：** 如果使用PyTorch实现的DeepFM，浏览关键代码（如FM层的二阶项计算、Embedding拼接等）fancyerii.github.io以理解内部机制。
     通过实战操作，将抽象的DeepFM概念变为具体体验，理解深度模型如何提升CTR预估效果。

## Day 4: 用户兴趣模型与DIN网络

- **理论学习目标：** 学习**深度兴趣网络（DIN）**模型，理解推荐系统中利用用户历史行为序列建模兴趣的策略。掌握注意力机制在CTR预估中的应用原理，以及DIN如何根据不同候选商品动态激活用户不同兴趣。了解DIN的改进动机和后续扩展（如DIEN）。
- **重点模型/概念：** **用户行为序列建模：** 传统深度CTR模型将用户历史行为编码为固定向量，未区别不同候选商品对应的兴趣[cloud.tencent.com](https://cloud.tencent.com/developer/article/1795015#:~:text=文章首先介绍了现有的点击率 ,Layer Perceptron) 中， 从而学习这些特征间的非线性关系。)。DIN通过引入**Attention机制**解决这一局限：对每个待推荐商品，针对用户历史点击序列计算注意力权重，只激活与该候选商品语义相关的用户兴趣[blog.csdn.net](https://blog.csdn.net/weixin_43290383/article/details/121345875#:~:text=)。DIN结构包括：Embedding层获取每个历史物品的向量表示；**本地激活单元（Attention层）\**根据候选商品，对历史行为的各物品赋予不同权重，得到\**加权兴趣向量**[blog.csdn.net](https://blog.csdn.net/weixin_43290383/article/details/121345875#:~:text=DIN核心思想可以归纳为： 1、 基于物品的推荐，也就是判断购物购买过的物品，进而判断是否和本广告物品相关 2、 用户购买过的物品，权重不一样。比如，推荐物品为奶粉，用户购买过笔记本电脑，购买过尿不湿，显然，尿不湿的奶粉的相关性高。因此需要提高尿不湿的权重。)[blog.csdn.net](https://blog.csdn.net/weixin_43290383/article/details/121345875#:~:text=,batch的正则化以及PReLU的更泛化的方法：Dice。下面首先介绍Base model。)；再与用户其他静态特征一起输入DNN预测CTR。DIN的核心贡献在于**捕捉用户兴趣多样性**，使得用户对不同商品的兴趣表示可动态变化[blog.csdn.net](https://blog.csdn.net/weixin_43290383/article/details/121345875#:~:text=CTR预估是在线广告以及电商的非常重要的业务，除了传统的方法之外，目前以embedding%2BMLP为主的深度学习方法也广为应用。 在这些方法中，用户特征用固定长度的向量来表示，而已有的方法往往忽视获取用户历史行为中的兴趣多样性，即：)。注意DIN还采用了Dice激活函数等技巧提高训练效果[blog.csdn.net](https://blog.csdn.net/weixin_43290383/article/details/121345875#:~:text=,batch的正则化以及PReLU的更泛化的方法：Dice。下面首先介绍Base model。)。另外可了解**Deep Interest Evolution Network (DIEN)** 对DIN的改进：用GRU+Attention捕捉兴趣随时间演化，但本次重点掌握DIN基本思想。
- **推荐资料：**
  - 论文：Zhou et al. *“Deep Interest Network for Click-Through Rate Prediction”* (2018)[blog.csdn.net](https://blog.csdn.net/weixin_43290383/article/details/121345875#:~:text=掌握2017年阿里提出的深度兴趣网络（Deep Interest Network for Click,Through Rate Prediction)（DIN原论文，提出了序列兴趣提取和局部激活单元）。
  - 知乎专栏：「推荐系统中的注意力机制——阿里深度兴趣网络（DIN）」[blog.csdn.net](https://blog.csdn.net/weixin_43290383/article/details/121345875#:~:text=CTR预估是在线广告以及电商的非常重要的业务，除了传统的方法之外，目前以embedding%2BMLP为主的深度学习方法也广为应用。 在这些方法中，用户特征用固定长度的向量来表示，而已有的方法往往忽视获取用户历史行为中的兴趣多样性，即：)[blog.csdn.net](https://blog.csdn.net/weixin_43290383/article/details/121345875#:~:text=,batch的正则化以及PReLU的更泛化的方法：Dice。下面首先介绍Base model。)（深入讲解DIN的核心思想和结构图示）。
  - 博客：「阿里DIN模型详解」[blog.csdn.net](https://blog.csdn.net/weixin_43290383/article/details/121345875#:~:text=DIN核心思想可以归纳为： 1、 基于物品的推荐，也就是判断购物购买过的物品，进而判断是否和本广告物品相关 2、 用户购买过的物品，权重不一样。比如，推荐物品为奶粉，用户购买过笔记本电脑，购买过尿不湿，显然，尿不湿的奶粉的相关性高。因此需要提高尿不湿的权重。)[blog.csdn.net](https://blog.csdn.net/weixin_43290383/article/details/121345875#:~:text=CTR预估是在线广告以及电商的非常重要的业务，除了传统的方法之外，目前以embedding%2BMLP为主的深度学习方法也广为应用。 在这些方法中，用户特征用固定长度的向量来表示，而已有的方法往往忽视获取用户历史行为中的兴趣多样性，即：)（通俗阐释为何引入注意力机制建模用户多样兴趣，并介绍DIEN等后续工作）。
- **实战任务：** 在阿里提供的电商推荐数据上尝试DIN模型：
  1. **准备数据：** 使用**阿里云天池**公开的阿里移动电商推荐数据[tianchi.aliyun.com](https://tianchi.aliyun.com/dataset/46#:~:text=阿里移动推荐算法数据集 )或天池新闻推荐数据，提取每个用户的历史点击序列和候选商品集合。
  2. **训练DIN：** 借助开源实现（如Alipay开源的EasyRec库或DeepCTR的DIN实现），输入用户行为序列、用户画像和候选商品特征，训练DIN模型预测点击率。
  3. **观察注意力输出：** 将某个用户的历史行为和一个候选商品送入模型，提取Attention层输出的权重，看看模型赋予哪些历史物品更高权重。例如，当候选是“奶粉”时，用户历史中“尿不湿”可能权重大于“笔记本”，符合直觉[blog.csdn.net](https://blog.csdn.net/weixin_43290383/article/details/121345875#:~:text=DIN核心思想可以归纳为： 1、 基于物品的推荐，也就是判断购物购买过的物品，进而判断是否和本广告物品相关 2、 用户购买过的物品，权重不一样。比如，推荐物品为奶粉，用户购买过笔记本电脑，购买过尿不湿，显然，尿不湿的奶粉的相关性高。因此需要提高尿不湿的权重。)。
  4. **对比基线：** 比较DIN与不使用注意力的等权重Pooling方案效果，体验DIN对CTR提升的实际收益。
     通过这一步的实践，理解如何动态建模用户兴趣，以及Attention机制在推荐系统中的威力。

## Day 5: 推荐系统召回策略与初识双塔模型

- **理论学习目标：** 理解推荐系统**召回阶段**的重要性和常用方法，区分召回与排序的侧重点。学习传统召回算法（协同过滤、内容召回）的原理和局限，引入工业界常用的**向量检索召回**。初步了解**双塔结构**模型在海量候选召回中的优势，为融合搜索技术做准备。
- **重点模型/概念：** **召回 vs 排序：** 召回负责从上百万物料中快速筛选相关候选，要求**高覆盖和高速**；排序负责对召回候选精细打分，要求**高精度**，因此召回用较简单模型、排序用复杂模型[developer.aliyun.com](https://developer.aliyun.com/article/1054418#:~:text=YoutubeDNN是Youtube用于做视频推荐的落地模型，可谓推荐系统中的经典，其大体思路为召回阶段使用多个简单模型筛除大量相关度较低的样本，排序阶段使用较 为复杂的模型获取精准的推荐结果。)[developer.aliyun.com](https://developer.aliyun.com/article/1054418#:~:text=match at L327 YoutubeDNN在双塔模型基础上进行了改进，召回阶段使用多个简单模型筛除大量相关度较低的样本，排序阶段使用较为复杂的模型获取精准的推荐结果。)。**协同过滤（CF）**：包括**UserCF**（找相似用户推荐[woshipm.com](https://www.woshipm.com/pd/4223123.html#:~:text=1）基于用户的协同过滤)）和**ItemCF**（找相似物品推荐[woshipm.com](https://www.woshipm.com/pd/4223123.html#:~:text=2）基于物品的协同过滤)），利用用户行为共现挖掘偏好。但CF冷启动差、无法处理新物品/新用户。**内容召回：** 基于物品内容属性（标签、文本等）计算相似度，解决冷启动但需高质量标签。**向量召回（ANN检索）**：利用深度模型把用户和物品映射到同一语义向量空间，在线上通过近似最近邻搜索获取Top K相似向量候选[cnblogs.com](https://www.cnblogs.com/arachis/p/DSSM.html#:~:text=自然能想到，将用户最近点击，消费，转化等的数据采集后作为正样本；采样些全局最热但未点击(Youtube负采样套路)作为负样本构造样本训练DSSM模型进行 推荐召回。 DSSM在预测的时候就可以借助Faiss进行异步物料向量更新，加载模型后可以根据实时用户行为进行ANN检索(求Topk相似向量)。 下图为视频 ,深度学习在美图个性化推荐的应用实践 中DSSM召回线下训练，线上serving的整体架构图。)。此类**双塔模型**架构下，用户塔和物品塔各自独立编码，预先计算并存储海量物品向量，实时查询时只需编码用户向量并近似匹配最近的物品向量，大幅提高召回效率[cnblogs.com](https://www.cnblogs.com/zhengzhicong/p/12862816.html#:~:text=以百度的双塔模型举例说明：)。这一架构通过**embedding召回**有效应对高并发和低延迟要求，是现代推荐召回的主流。
- **推荐资料：**
  - 博客：「推荐系统召回策略综述」 – 介绍UserCF/ItemCF算法公式及向量召回的基本概念，含Faiss近似NN搜索示例。
  - 博客：「DSSM在召回中的应用」[cnblogs.com](https://www.cnblogs.com/arachis/p/DSSM.html#:~:text=DSSM)[cnblogs.com](https://www.cnblogs.com/arachis/p/DSSM.html#:~:text=2)（阐述双塔模型用于推荐召回，及利用Faiss做异步向量更新和Top K搜索[cnblogs.com](https://www.cnblogs.com/arachis/p/DSSM.html#:~:text=自然能想到，将用户最近点击，消费，转化等的数据采集后作为正样本；采样些全局最热但未点击(Youtube负采样套路)作为负样本构造样本训练DSSM模型进行 推荐召回。 DSSM在预测的时候就可以借助Faiss进行异步物料向量更新，加载模型后可以根据实时用户行为进行ANN检索(求Topk相似向量)。 下图为视频 ,深度学习在美图个性化推荐的应用实践 中DSSM召回线下训练，线上serving的整体架构图。)）。
  - 知乎回答：「协同过滤的原理」 – 图解UserCF和ItemCF的“找相似”过程，加深对协同过滤直观理解。
- **实战任务：** 体验两种典型召回方式：协同过滤 vs 向量召回：
  1. **协同过滤实现：** 使用MovieLens数据集，用UserCF为每个用户召回电影候选。计算用户相似度（如余弦相似），对某用户取与其最相似的一批用户喜欢的电影作为候选。实现简易版本，体会协同过滤逻辑。
  2. **Embedding训练：** 在同一数据上训练一个简化的双塔模型：例如用矩阵分解/Word2Vec方法，将用户和物品嵌入到同一向量空间。可以将用户的历史观看序列当作“句子”，用word2vec训练**item embedding**，再为用户取其历史物品向量平均作为**user embedding**。
  3. **向量召回实验：** 利用Faiss库，将所有物品embedding构建索引。随机选择若干用户，取其user embedding在索引中查找Top K相近的item向量，得到候选推荐列表。
  4. **结果对比：** 比较Embedding召回结果与协同过滤结果，对照推荐电影清单，看哪种方法获得了更加多样或精准的候选。考虑热门物品对结果的影响，是否需要引入热门度平衡。
     通过动手实现，理解传统CF召回与新兴向量召回的差异，体会双塔向量召回在效率和效果上的优势，为深入学习双塔结构和搜索融合打下基础。

## Day 6: 双塔深度匹配模型DSSM与语义召回实践

- **理论学习目标：** 深入学习**DSSM（深度结构化语义模型）**[developer.aliyun.com](https://developer.aliyun.com/article/1054418#:~:text=DSSM,学习网络将query和doc映射到相同维度的语义空间中，即query侧特征的embedding和doc侧特征的embedding，从而得到语句的低维语义向量表达 sentence embedding，用于预测两句话的语义相似度。)作为典型双塔模型的结构与原理，了解其在搜索和推荐中的语义匹配应用。掌握双塔架构如何表征 Query 和 Document（或用户和物品），以及通过语义向量空间计算相关度。理解DSSM在召回和粗排阶段的实际工业应用案例。
- **重点模型/概念：** **DSSM模型：** 由微软提出的双塔网络，将查询和文档分别输入各自的DNN（共享或不共享参数），映射到相同维度的向量表示，再通过**向量相似度**（余弦或内积）衡量语义相关性[cnblogs.com](https://www.cnblogs.com/arachis/p/DSSM.html#:~:text=paper%3ALearning Deep Structured Semantic Models,Search using Clickthrough Data example%3ADSSM召回模型)。原始DSSM用于Web搜索的查询-文档语义相关度计算，通过点击日志训练，把点击过的query-doc作为正例[cnblogs.com](https://www.cnblogs.com/arachis/p/DSSM.html#:~:text=原始的DSSM是在搜索CTR预估任务，大概分为：1)embedding层 2)MLP层 3)cosine相似度logit 4)softmax层；其中Q代表搜索词(用户)，D1%2C2%2C)。**双塔架构优势：** 查询塔与文档塔独立计算，适合离线批量算出海量文档向量，在召回阶段用ANN高效匹配[cnblogs.com](https://www.cnblogs.com/arachis/p/DSSM.html#:~:text=自然能想到，将用户最近点击，消费，转化等的数据采集后作为正样本；采样些全局最热但未点击(Youtube负采样套路)作为负样本构造样本训练DSSM模型进行 推荐召回。 DSSM在预测的时候就可以借助Faiss进行异步物料向量更新，加载模型后可以根据实时用户行为进行ANN检索(求Topk相似向量)。 下图为视频 ,深度学习在美图个性化推荐的应用实践 中DSSM召回线下训练，线上serving的整体架构图。)。这架构被广泛应用于推荐系统的向量召回和粗排，将用户特征和物品特征分别编码后计算相似度[cnblogs.com](https://www.cnblogs.com/arachis/p/DSSM.html#:~:text=DSSM)[cnblogs.com](https://www.cnblogs.com/arachis/p/DSSM.html#:~:text=原始的DSSM是在搜索CTR预估任务，大概分为：1)embedding层 2)MLP层 3)cosine相似度logit 4)softmax层；其中Q代表搜索词(用户)，D1%2C2%2C)。工业应用中，DSSM常用于：**召回阶段**训练用户塔和物品塔，实时召回Top K候选；**粗排阶段**作为精排前的轻量模型，用学生网络蒸馏复杂精排模型[cnblogs.com](https://www.cnblogs.com/arachis/p/DSSM.html#:~:text=3)。需注意双塔模型训练时**负样本选择**技巧（如随机负采样、热门采样）对效果影响较大[cnblogs.com](https://www.cnblogs.com/arachis/p/DSSM.html#:~:text=自然能想到，将用户最近点击，消费，转化等的数据采集后作为正样本；采样些全局最热但未点击(Youtube负采样套路)作为负样本构造样本训练DSSM模型进行 推荐召回。 DSSM在预测的时候就可以借助Faiss进行异步物料向量更新，加载模型后可以根据实时用户行为进行ANN检索(求Topk相似向量)。 下图为视频 ,深度学习在美图个性化推荐的应用实践 中DSSM召回线下训练，线上serving的整体架构图。)。
- **推荐资料：**
  - 博客：「微软DSSM模型原理」[chenhuiyu.github.io](https://chenhuiyu.github.io/2023/07/07/NLP Insights/DSSM 模型详解/index.html#:~:text=DSSM（Deep Structured Semantic Models）是微软提出的一个深度学习模型，用于学习文本的语义表达。DSSM首次在信息检索领域中被提出，用于处理查询)（讲解DSSM网络结构和在文本匹配中的语义表示方法）。
  - 博客：「DSSM在召回和粗排的应用举例」[cnblogs.com](https://www.cnblogs.com/arachis/p/DSSM.html#:~:text=DSSM)[cnblogs.com](https://www.cnblogs.com/arachis/p/DSSM.html#:~:text=原始的DSSM是在搜索CTR预估任务，大概分为：1)embedding层 2)MLP层 3)cosine相似度logit 4)softmax层；其中Q代表搜索词(用户)，D1%2C2%2C)（介绍DSSM双塔用于推荐召回/粗排的架构，并讨论线上部署、蒸馏加速等工程细节）。
  - 论文：Huang et al. *“Learning Deep Structured Semantic Models for Web Search”* (CIKM 2013) – DSSM原始论文，了解其在搜索场景用字词三元组输入网络的创新。
- **实战任务：** 构建并训练简易双塔模型进行语义匹配任务：
  1. **选取数据：** 使用一个问答匹配数据集（如**Quora问题对**或中文**LCQMC问句匹配数据集**）。该任务需要判断两个句子是否语义相似，非常适合训练双塔模型。
  2. **设计模型：** 模仿DSSM结构，建立“Siamese”双塔网络：两个塔共享参数（同样的文本编码网络，例如多层全连接或简化的BERT编码），输出固定维度向量。计算两个向量的余弦相似度，用作匹配分数。训练目标为让正例(query, doc)相似度高于负例。
  3. **训练模型：** 用PyTorch训练上述网络。由于BERT训练较慢，可先用词袋或简单Embedding+平均池化作为编码层，确保模型可在4-5小时内收敛。采用**Triplet Loss**或**对比损失**进行训练（正对和随机负对）。
  4. **验证效果：** 取若干查询，在验证集中用模型向量检索相似问题或答案。观察模型是否能在语义上匹配出相关的问题（例如“如何减少损失”应匹配“怎样降低亏损”而非字面相似的无关问题）。
  5. **思考优化：** 尝试增加塔的网络深度或引入预训练词向量，观察对匹配效果的提升；了解实际工业应用中可能使用预训练模型（如BERT双塔）进一步增强语义表示能力。
     通过此简化实验，加深对双塔语义匹配模型的理解，亲身体验其在处理文本语义检索（可类比推荐场景的向量召回）中的效果和挑战。

## Day 7: 搜索排序模型与BERT语义匹配

- **理论学习目标：** 学习搜索引擎的排序机制和引入语义匹配的现代方法。理解传统搜索（基于关键词TF-IDF/BM25）的局限，以及BERT等预训练模型如何提升搜索相关性。掌握**语义检索**的概念：利用深度语义模型理解用户查询意图和文档含义，实现同义词匹配和智能问答。重点了解**BERT在搜索排序中的应用**，包括双塔与交叉交互两种模式，以及实际工程权衡。
- **重点模型/概念：** **传统搜索排序**：依赖词频和倒排索引，快速定位包含关键词的文档，但无法理解语义（同义词匹配差、难以理解长 query 意图）[blog.csdn.net](https://blog.csdn.net/2501_91651657/article/details/148074803#:~:text=一、语义搜索的发展需求与传统算法的局限)。**语义搜索**：引入预训练语言模型（如BERT）的语义理解能力，将搜索引擎从关键词匹配提升到语义匹配时代[blog.csdn.net](https://blog.csdn.net/2501_91651657/article/details/148074803#:~:text=在传统搜索引擎时代，基于关键词匹配和简单统计的算法虽能满足基础检索需求，但面对复杂语义查询时往往力不从心。随着自然语言处理技术的突破，以BERT（Bidirec tional Encoder Representations from,Transformers）为代表的预训练模型横空出世，推动搜索引擎进入语义搜索时代。这些 模型凭借强大的语义理解能力，深刻改变了搜索引擎底层算法逻辑，显著提升了用户检索体验。)。**BERT双塔**（Bi-Encoder）**vs BERT交叉**（Cross-Encoder）：双塔方式用BERT分别编码query和doc为向量，在语义向量空间做近邻检索，效率高但精度略低；交叉方式将query和doc拼接输入BERT做匹配分类，精度高但实时计算慢，常用于精排。**BERT语义匹配**在搜索中的应用包括：查询理解（将查询编码为向量以查找相似历史查询）[blog.csdn.net](https://blog.csdn.net/2501_91651657/article/details/148074803#:~:text=1)、文档表征与召回（以BERT向量表示文档语义，与查询向量算余弦得到相关度，并结合BM25分值混排[blog.csdn.net](https://blog.csdn.net/2501_91651657/article/details/148074803#:~:text=2)）、以及**智能问答**（BERT提取文本片段直接回答问题[blog.csdn.net](https://blog.csdn.net/2501_91651657/article/details/148074803#:~:text=3)）。了解实际案例：如**Meituan**将BERT相关性模型作为特征融入排序，提高了点击率[tech.meituan.com](https://tech.meituan.com/2020/07/09/bert-in-meituan-search.html#:~:text=BERT在美团搜索核心排序的探索和实践 BERT相关性模型和排序模型的深入融合：当前两个模型属于两阶段训练方式，将BERT语义相关性作为特征加入排序模型来提升点击率。语义相关性是影响搜索体验的 )；**Google**在搜索排序中融合BERT以更好理解自然语言查询。还需关注**性能挑战**：BERT模型参数巨大，在线推理代价高，需蒸馏压缩或多级检索架构确保时延可控[blog.csdn.net](https://blog.csdn.net/2501_91651657/article/details/148074803#:~:text=1)。
- **推荐资料：**
  - 技术博客：「进入BERT时代，向量语义检索关注什么」[zhuanlan.zhihu.com](https://zhuanlan.zhihu.com/p/183952345#:~:text=知乎)（讨论BERT语义匹配中的负采样和应用要点）。
  - 美团技术团队：「BERT在美团搜索排序的实践」[tech.meituan.com](https://tech.meituan.com/2020/07/09/bert-in-meituan-search.html#:~:text=BERT在美团搜索核心排序的探索和实践 BERT相关性模型和排序模型的深入融合：当前两个模型属于两阶段训练方式，将BERT语义相关性作为特征加入排序模型来提升点击率。语义相关性是影响搜索体验的 )（介绍如何将BERT生成的语义相关性特征融合进搜索排序模型提升相关性[tech.meituan.com](https://tech.meituan.com/2020/07/09/bert-in-meituan-search.html#:~:text=BERT相关性模型和排序模型的深入融合：当前两个模型属于两阶段训练方式，将BERT语义相关性作为特征加入排序模型来提升点击率。语义相关性是影响搜索体验的 )）。
  - CSDN博文：「语义搜索时代：BERT等预训练模型在搜索引擎算法中的应用」[blog.csdn.net](https://blog.csdn.net/2501_91651657/article/details/148074803#:~:text=在传统搜索引擎时代，基于关键词匹配和简单统计的算法虽能满足基础检索需求，但面对复杂语义查询时往往力不从心。随着自然语言处理技术的突破，以BERT（Bidirec tional Encoder Representations from,Transformers）为代表的预训练模型横空出世，推动搜索引擎进入语义搜索时代。这些 模型凭借强大的语义理解能力，深刻改变了搜索引擎底层算法逻辑，显著提升了用户检索体验。)[blog.csdn.net](https://blog.csdn.net/2501_91651657/article/details/148074803#:~:text=2)（系统阐述传统搜索局限与BERT应用场景，如查询理解、文档重排、智能问答）。
- **实战任务：** 将BERT应用于一个小型语义检索场景：
  1. **数据准备：** 选取一个小型问答对集合或FAQ文档集。（例如一份常见问题列表，每个问题有一个答案。）
  2. **离线向量检索：** 使用预训练的中文**Sentence-BERT**模型，通过HuggingFace接口，将FAQ集合中的每个问题编码成向量，并存入向量索引（可用Faiss）。
  3. **在线查询测试：** 设计若干测试查询，人工编写对应的正确答案。用同样的BERT编码获取查询向量，在索引中寻找最相似的FAQ问题，返回其答案。观察BERT语义检索是否能找出语义接近的问答对。例如查询“账号忘记密码怎么办”，是否能匹配到FAQ中的“如何找回密码”。
  4. **结果分析：** 对比检索结果与传统关键词匹配的结果（可以用简单关键词搜索作为对照）。BERT向量检索应能找出包含不同措辞但语义相似的问答，而关键词法可能错失这些[blog.csdn.net](https://blog.csdn.net/2501_91651657/article/details/148074803#:~:text=早期搜索引擎依赖TF)。记录几个成功和失败的例子，分析BERT模型的语义理解能力和局限，如是否出现词语量纲不准导致误匹配。
  5. **优化实践：** 若时间允许，尝试**微调BERT**：将部分问答对作为训练集，对预训练BERT做语义相似度微调，使其更贴合特定领域，提高检索准确率。
     通过本实验，熟悉BERT在语义匹配中的用法，体会其相较传统搜索算法的提升，以及应用中的性能挑战。在实践中理解预训练模型如何赋能搜索引擎，使查询与文档的匹配更智能精准。

## Day 8: Transformer在推荐中的应用与推荐搜索融合

- **理论学习目标：** 扩展视野到**Transformer架构**在推荐领域的新进展，以及推荐与搜索技术融合的前沿探索。学习**序列推荐模型**（如SASRec、BERT4Rec）如何借鉴NLP中的Transformer机制提升推荐效果。了解业界在统一搜索和推荐框架方面的尝试，以及多任务学习在搜索和推荐融合中的应用。
- **重点模型/概念：** **序列推荐Transformer模型：** 代表如**SASRec** (Self-Attentive Sequential Rec) 利用自注意力机制建模用户行为序列，是序列推荐算法的重要Baseline[cloud.tencent.com](https://cloud.tencent.com/developer/article/1795015#:~:text=> 论文：Self)。SASRec的核心是在Encoder结构中用多头注意力捕捉长期偏好，并将用户历史表示与候选物品embedding做内积，筛选Top K推荐[cloud.tencent.com](https://cloud.tencent.com/developer/article/1795015#:~:text=> 论文：Self)。后续还有**BERT4Rec**直接引入双向Transformer预测序列中掩盖的下一个物品。Transformer结构在推荐中的应用可以更好捕捉序列中元素的远距离依赖和相对位置影响。**推荐-搜索融合方法：** 在工业场景中，推荐和搜索逐步出现融合趋势。例如**统一召回**：Spotify的联合生成检索模型同时在搜索和推荐数据上训练，通过多任务学习提升召回效果[blog.csdn.net](https://blog.csdn.net/2401_86652632/article/details/146373614#:~:text=)。**统一架构**：LinkedIn提出统一模型处理多样检索与排序任务，Netflix的「悉统一（UniCoRn）」模型也是融合推荐搜索的尝试[blog.csdn.net](https://blog.csdn.net/2401_86652632/article/details/146373614#:~:text=)。此外，**互相赋能**：快手将用户搜索行为融入推荐模型（如将搜索词作为用户兴趣特征），显著提高推荐效果[zhuanlan.zhihu.com](https://zhuanlan.zhihu.com/p/679340800#:~:text=快手破局：巧借因果链，融合搜索数据赋能推荐模型新高度 )。这些探索体现了将搜索的**显式意图**与推荐的**隐式偏好**相结合的思路，或使用一个模型框架同时服务搜索和推荐两种场景，从而**提升效果并降低系统复杂度**[blog.csdn.net](https://blog.csdn.net/2401_86652632/article/details/146373614#:~:text=统一的系统架构，将搜索和推荐融合在同一基础设施内，正逐渐成为趋势。LinkedIn的360Brew和Netflix的UniCoRn等模型，展示了统一模型在多任务 训练中的优势，能够同时处理多样化的检索和排名任务。)。
- **推荐资料：**
  - 博客：「SASRec：基于自注意力的序列推荐」[cloud.tencent.com](https://cloud.tencent.com/developer/article/1795015#:~:text=> 论文：Self)（介绍SASRec设计思想，自注意力如何应用于用户序列建模）。
  - 论文：Sun et al. *“BERT4Rec: Sequential Recommendation with Bidirectional Encoder Representations”* (CIKM 2019) – 了解Transformer预训练在推荐场景的移植。
  - 技术分享：「搜索与推荐的融合创新」[blog.csdn.net](https://blog.csdn.net/2401_86652632/article/details/146373614#:~:text=)（讨论Spotify、LinkedIn等在统一检索排序模型方面的实践，以及LLM时代对搜推融合的展望）。
- **实战任务：** 结合本周所学，完成一次小型综合项目，将推荐和搜索方法融合：
  1. **场景定义：** 选择一个模拟场景，例如一个资讯推荐+搜索系统，既有用户无明确查询浏览新闻的推荐模块，也有用户输入关键词检索新闻的搜索模块。准备一个包含文章内容、主题标签的数据集。
  2. **统一表示学习：** 训练一个Transformer模型生成文章的语义表示。可以采用BERT微调或简化的自注意力网络，以文章标题/内容训练一个向量表示，用于后续无论搜索或推荐的匹配。
  3. **推荐模块：** 利用序列推荐思想，为每个用户生成一个“用户查询向量”（可通过汇总用户最近阅读的几篇文章的向量得到）。然后用向量近邻检索为用户推荐Top N相关文章（类似Day7语义检索，但这里query由用户历史生成而非显式输入）。
  4. **搜索模块：** 当用户输入关键词时，用同一模型编码查询为向量，在文章向量库中检索最近邻作为搜索结果（可以同时结合简单BM25打分排序混排）。
  5. **融合分析：** 比较推荐结果与搜索结果的异同：推荐结果是否贴合用户历史兴趣，搜索结果是否准确回答查询。同时思考，如果让系统同时参考用户兴趣和查询意图（例如用户在看某类内容时进行搜索，如何结合两者提供结果），这就是搜推融合可以努力的方向。
  6. **总结提升：** 记录本项目中Transformer表示学习的效果，遇到的困难（如冷启动、新主题文章表示等），思考通过预训练模型、大模型（如ChatGPT）生成特征等方法改进的可能。
     通过综合项目，将Transformer驱动的表示学习贯穿推荐和搜索，亲身体验两者技术融合的雏形。这不仅巩固了对Transformer在信息检索中应用的认识，也为未来搜索推荐一体化的思考埋下伏笔。

## Day 9: 项目实战（I）——个性化推荐算法比赛练习

- **理论学习目标：** 将前一阶段所学应用到真实数据竞赛场景中。通过参与一次推荐算法比赛（Kaggle或天池），提升大规模数据处理、特征工程和模型调优能力。学会阅读和借鉴优秀方案，在实战中发现知识盲区并查漏补缺。
- **重点模型/概念：** 强化对**完整推荐模型训练流程**的认识：包括数据探索与清洗、特征提取（用户画像、物品属性、交互行为等）、训练集构建、模型选择与调参、离线验证以及结果分析。具体概念根据所选比赛可能涉及：**长短期兴趣结合**、**多目标优化**（例如同时优化点击率和转化率）、**模型融合**（blending多模型提高效果），以及**线上部署考虑**（如推理效率）。
- **选择比赛/数据：** 推荐参加**阿里云天池**或**Kaggle**上与CTR预估或推荐相关的比赛。例如：天池举办的“阿里移动推荐算法大赛”[tianchi.aliyun.com](https://tianchi.aliyun.com/dataset/46#:~:text=阿里移动推荐算法数据集 )（预估用户对商品的点击或购买行为）或Kaggle的“Display Advertising Challenge”（Criteo CTR预估）。这些比赛提供真实海量数据，非常贴近岗位要求。
- **推荐资料：**
  - 天池比赛链接：[tianchi.aliyun.com](https://tianchi.aliyun.com/dataset/46#:~:text=阿里移动推荐算法数据集 )“阿里移动推荐算法大赛”（含比赛数据说明和Baselines）。
  - Kaggle比赛页面：*Display Advertising Challenge (Avazu)* 或 *Criteo Labs Ad CTR*（查看讨论区优秀方案分享）。
  - 博客：「某推荐比赛Top方案解析」 – 搜索比赛名称+top solution，学习冠军队特征工程和模型融合技巧。
- **实战任务：** 模拟参赛流程，在两天内完成一个小型推荐项目：
  **Day9-数据与特征工程：**
  1. **数据分析：** 下载比赛数据，使用pandas等工具浏览数据分布，如用户活跃度、物品热门度、行为序列长度等，形成基本认识。
  2. **数据处理：** 针对数据特点进行清洗和转化。例如处理缺失值，将时间戳转换为分类特征（如星期几、时间段），对高基数ID特征考虑频次截断等。
  3. **特征工程：** 构造初步特征集合，包括基本特征（用户ID、物品ID、类别等）、统计特征（用户点击某类物品次数等）、序列特征（最近浏览序列）等。应用之前所学，如用户行为序列长度、时间间隔等可能作为特征供模型使用。
  4. **划分数据集：** 按时间或随机划分训练集、验证集，确保验证集能评估模型泛化性能。
  5. **Baseline建模：** 先训练一个简易模型作为Baseline，例如LR或矩阵分解，快速得到一个初始评分（AUC/logloss），为后续改进提供参考。
  6. **记录Observations：** 写下数据中的挑战（如数据极度不平衡、冷启动比例）和可能的解决思路（如采样策略、特征补充），为第二天建模做计划。
     通过Day9的准备，确保对比赛问题有深入理解，并搭建好数据处理pipeline。为第二天模型实现和优化打好基础。

## Day 10: 项目实战（II）——模型构建与优化

- **理论学习目标：** 在真实比赛数据上训练和优化一个**推荐模型**，巩固模型调参和性能提升技巧。学会结合多种模型（如Wide&Deep、DeepFM、DIN等）的长处，提高预测效果。理解**模型评估**与**过拟合**的现象及应对方法。
- **重点模型/概念：** 根据Day9分析，选取合适的模型或模型组合：若数据以CTR预估为目标且特征丰富，可选择**DeepFM**或**XGBoost+LR**组合；若有用户行为序列，可引入**DIN/DIEN**捕捉兴趣；若多任务目标，可考虑**ESMM**或多塔结构。重点关注**过拟合**迹象（训练AUC远高于验证AUC），引入**正则化、Dropout、早停**等技巧改进泛化。也可以尝试**模型融合**（blending），如将矩阵分解召回得分作为特征加入深度模型。通过优化迭代，体验工业模型从baseline逐步提升的过程。
- **推荐资料：**
  - 深度学习CTR模型调参技巧汇总 – 探讨网络结构（层数、激活）、正则项、BatchNorm等对模型的影响。
  - 论文：**“Facebook Xavier: Practical Lessons from Predicting Clicks on Ads at Facebook”** – 了解业界在超大规模CTR预估上的工程经验（如分布式训练、稀疏优化等），为面试提供谈资。
  - 比赛优秀解法分享 – 比如Kaggle论坛中Top1 solution，关注其特征处理和模型设计思想。
- **实战任务：** **Day10-模型训练与迭代：**
  1. **模型实现：** 基于昨日特征，训练一个**DeepFM**模型。使用PyTorch的高阶库（如DeepCTR-Torch）加速实现，设置合理的Embedding维度和MLP结构。监控训练过程中的logloss/AUC曲线，避免过拟合。
  2. **性能调优：** 如果验证效果不佳，分析原因：是欠拟合（考虑增加网络深度或特征多样性）还是过拟合（增加L2正则、Dropout或收集更多训练数据）。有序地调整超参数并记录结果。例如尝试embedding维度从8增至16，观察AUC变化。
  3. **加入序列模型：** 若数据存在用户历史序列，尝试将DIN模型融入：提取用户最近N次行为，用Attention生成兴趣向量融入DeepFM。对比加入DIN模块前后的效果提升，验证序列建模的价值。
  4. **模型融合：** 训练一个梯度提升树模型（XGBoost/LightGBM）使用部分人工特征，看看AUC表现。如有提升，可将树模型与深度模型预测做加权平均或作为stacking第二层，提高最终成绩。
  5. **离线评估：** 使用验证集计算最终模型指标，并与Baseline比较提升幅度。同时检查不同用户分群的效果，是否均有改善。
  6. **提交测试（可选）：** 如为Kaggle/天池比赛，可将结果提交排行榜，了解模型在真实测试集的表现。
     通过完整的项目实战，您将体会从0到1构建推荐算法的过程，锻炼处理真实业务数据和持续改进模型的能力。这也是面试官非常看重的实践经验。

## Day 11: 综合知识复盘与重点难点梳理

- **理论学习目标：** 对过去10天学习的所有核心知识进行系统梳理，总结每个主题的关键要点和相互联系。补充之前可能一带而过的概念，确保没有知识盲区。构建**知识网络**，方便面试快速调取答案。
- **复盘内容：**
  - **推荐系统总体架构：** 能够口述一个推荐系统从用户请求到物料推荐的完整流程，包括候选生成（召回）、多路召回融合、排序、多次重排序/过滤等[woshipm.com](https://www.woshipm.com/pd/4223123.html#:~:text=,排序层：利用机器学习或者深度学习模型，以及更丰富的特征进行重排序，筛选出更小、更精准的推荐集合返回给上层业务。)。对于每层的技术要点（如召回用ANN提高速度、排序用CTR模型提高精度）心中有数。
  - **CTR预估与模型演进：** 梳理从LR、FM、Wide&Deep、DeepFM到DIN的演进脉络[cnblogs.com](https://www.cnblogs.com/zhengzhicong/p/12862816.html#:~:text=7)。总结每个模型解决的问题（如FM解决特征组合爆炸、DeepFM免特征工程、DIN建模兴趣序列）以及它们的结构特点。牢记关键区别和优点，如Wide&Deep vs DeepFM、DeepFM vs xDeepFM、DIN vs DIEN。
  - **召回与搜索融合：** 整理召回方式：协同过滤 vs 向量召回的差异和适用场景。再次确认双塔模型原理，用自己的话解释**“双塔结构如何提升召回效率”**[cnblogs.com](https://www.cnblogs.com/zhengzhicong/p/12862816.html#:~:text=以百度的双塔模型举例说明：)。回顾DSSM如何同时用于推荐召回和搜索粗排，以及BERT语义检索如何提高搜索相关性[blog.csdn.net](https://blog.csdn.net/2501_91651657/article/details/148074803#:~:text=2)。思考推荐和搜索技术融合的实例，准备一个例子说明（如Spotify多任务模型提升召回率[blog.csdn.net](https://blog.csdn.net/2401_86652632/article/details/146373614#:~:text=统一的系统架构，将搜索和推荐融合在同一基础设施内，正逐渐成为趋势。LinkedIn的360Brew和Netflix的UniCoRn等模型，展示了统一模型在多任务 训练中的优势，能够同时处理多样化的检索和排名任务。)或快手利用搜索行为改进推荐[zhuanlan.zhihu.com](https://zhuanlan.zhihu.com/p/679340800#:~:text=快手破局：巧借因果链，融合搜索数据赋能推荐模型新高度 )）。
  - **其他知识点：** 列出岗位要求中提及的概念，如Transformer、BERT匹配、DeepFM、DSSM等，逐一写出1-2句话解释定义，以确保面试回答言简意赅。例如“Transformer是谷歌提出的基于自注意力的编码器解码器架构，在推荐中用于建模长序列依赖[cloud.tencent.com](https://cloud.tencent.com/developer/article/1795015#:~:text=> 论文：Self)”。重点准备“Attention机制是什么，起什么作用？”、“Embedding是什么，为什么要用Embedding？”这类基础高频问答。
- **推荐资料：**
  - 个人笔记：将每日日学到的内容整理成**思维导图**或表格，汇总核心公式（如FM的二阶项计算公式、AUC定义等）、模型结构图和关键词。
  - 面经资料：阅读**《推荐算法Engineer面试知识点总结》**博客或论坛贴，了解其他应聘者整理的知识要点，检查是否有遗漏（例如常见的业务指标、探索性问题等）。
  - QA平台：浏览知乎或SegmentFault上的“推荐系统 面试”问答，看看业内经典问题，如“如何解决推荐系统的冷启动”、“如何做多目标排序”等，构思自己的回答框架。
- **实战任务：** **知识复盘与整理：**
  1. **口述演练：** 面对镜子或打开录音，用5-10分钟不看资料地口头讲解“什么是DeepFM，它相对Wide&Deep的改进”[cnblogs.com](https://www.cnblogs.com/zhengzhicong/p/12862816.html#:~:text=4)、“DIN模型为啥引入Attention”[blog.csdn.net](https://blog.csdn.net/weixin_43290383/article/details/121345875#:~:text=CTR预估是在线广告以及电商的非常重要的业务，除了传统的方法之外，目前以embedding%2BMLP为主的深度学习方法也广为应用。 在这些方法中，用户特征用固定长度的向量来表示，而已有的方法往往忽视获取用户历史行为中的兴趣多样性，即：)等问题。听取录音，检查逻辑是否清晰，用词是否专业准确。如果某概念讲得磕绊，标记出来再复习相关资料直到讲通顺。
  2. **书面总结：** 写一份“**高频考点速览**”笔记（1-2页A4）。每个重点概念用一两句话总结，并尽量公式+文字结合。例如：FM公式、DeepFM结构图、DSSM双塔示意图等。在纸上默写关键公式（如交叉熵loss，AUC计算，FM二阶项公式），加强记忆。
  3. **难点攻克：** 回顾实践中遇到的难点问题，思考面试可能深入追问。例如项目中模型过拟合的问题，准备回答“如何通过正则化和Dropout缓解”；对于工业应用，如“如何在**延迟要求**下部署深度模型”，结合所学双塔方案[cnblogs.com](https://www.cnblogs.com/zhengzhicong/p/12862816.html#:~:text=以百度的双塔模型举例说明：)给出思路。
  4. **模拟笔试：** 找一些往年面试笔试题或在线测试（如leetcode关于推荐系统设计的讨论题），限时完成，以检验综合运用能力。
     通过全面复盘，构建自己的知识框架，确保在面试中无论问到哪个环节都有话可说、有理可依。

## Day 12: 模拟面试专项练习

- **理论学习目标：** 将所学知识应用于模拟真实面试情境。练习**自我介绍结合项目经历**的表述方式，准备针对推荐/搜索岗位的常见问题的回答，并进行现场问题的思路整理。通过模拟白板&编码环节，提升临场应对能力。
- **模拟内容安排：**
  - **环节1：项目讲解** – 假设面试官让你介绍一个做过的推荐系统相关项目。可将Day9-10的比赛项目或以往实习项目作为素材，练习用STAR法描述背景、你做了什么、取得的效果、遇到的挑战及解决。突出你在项目中应用的算法（如DeepFM、DIN）和取得的指标提升，用数据说话。准备好如何回答深入细节，例如“特征如何选择的？”“为什么选这个模型？有没有试其他模型？” 等。
  - **环节2：概念问答** – 让朋友扮演面试官随机提问技术细节，你快速回应。例如：
    - *问:* “DeepFM中的FM部分和Deep部分如何共同作用？”
      *答:* “FM部分通过二阶特征组合捕获线性交互，Deep部分用DNN捕获高阶非线性模式，二者共同训练、最后输出加权和[cnblogs.com](https://www.cnblogs.com/zhengzhicong/p/12862816.html#:~:text=4)。相比Wide&Deep，DeepFM自动学习了特征交互，无需手工设计Wide部分特征。”
    - *问:* “DSSM为什么适合用作推荐召回？”
      *答:* “因为DSSM采用双塔结构，将用户和物品映射到同一向量空间，用近似最近邻搜索即可高效从百万物品中召回Top K相关候选[cnblogs.com](https://www.cnblogs.com/arachis/p/DSSM.html#:~:text=自然能想到，将用户最近点击，消费，转化等的数据采集后作为正样本；采样些全局最热但未点击(Youtube负采样套路)作为负样本构造样本训练DSSM模型进行 推荐召回。 DSSM在预测的时候就可以借助Faiss进行异步物料向量更新，加载模型后可以根据实时用户行为进行ANN检索(求Topk相似向量)。 下图为视频 ,深度学习在美图个性化推荐的应用实践 中DSSM召回线下训练，线上serving的整体架构图。)。它预先计算物品embedding，线上只需计算用户塔，极大降低延迟[cnblogs.com](https://www.cnblogs.com/zhengzhicong/p/12862816.html#:~:text=以百度的双塔模型举例说明：)。”
    - *问:* “在推荐系统中如何利用BERT？”
      *答:* “BERT可用于**语义匹配**和**内容理解**。例如将商品文描或评论编码成向量，用于冷启动内容推荐；或者用BERT做query和item的交叉匹配提升精排相关性[blog.csdn.net](https://blog.csdn.net/2501_91651657/article/details/148074803#:~:text=2)。但要注意BERT计算量大，通常采用离线预计算或蒸馏小模型部署[blog.csdn.net](https://blog.csdn.net/2501_91651657/article/details/148074803#:~:text=1)。”
    - *问:* “如果线上系统需要毫秒级响应，你如何优化深度模型？”
      *答:* “首先可采用双塔结构预先计算embedding减少实时计算[cnblogs.com](https://www.cnblogs.com/zhengzhicong/p/12862816.html#:~:text=以百度的双塔模型举例说明：)；其次压缩模型如剪枝、量化；或者采用多级架构，先粗排筛选少量候选再用复杂模型精排，兼顾性能和效果。”
  - **环节3：算法与代码** – 面试也可能涉及算法题或简单代码考查。练习1-2道LeetCode简单题，尤其是与数组、字符串处理相关的（互联网常考基础）。同时，准备一道“设计题”，例如“如何设计YouTube的视频推荐系统？”。回答时从用户数据、召回策略（协同过滤+YouTube DNN）、排序模型（LR+深度模型）、用户新鲜度、多样性等方面系统作答，展现对全局的把握。
- **推荐资料：**
  - 《机器学习面试精选200题》 – 包含推荐系统和深度学习常见问答，可用来自测。
  - YouTube视频：“Google推荐系统面试模拟” – 观摩他人如何回答设计类问题，学习组织语言和拆解问题的方法。
  - 校招笔试真题整理 – 看看往年推荐/广告算法岗笔试考过的选择题或简答题，做到心中有数。
- **实战任务：** **模拟面试：**
  找朋友或学长进行一场全真的模拟面试（约1小时）：
  1. **自我介绍及项目**（5分钟）：练习用精炼语言介绍自己的背景以及与推荐搜索相关的项目经历，突出亮点和个人贡献。
  2. **技术问答**（30分钟）：朋友根据准备的问题清单随机提问，也可即兴追问。你着重练习思维清晰地阐述要点，必要时可在纸上画示意图（如双塔结构图）辅助说明。对于不确定的问题，练习冷静分析推理出答案或诚实说明思考方向。
  3. **编码环节**（15分钟）：让朋友出一道简单算法题，当场用纸笔或在线共享文档写出核心代码。练习边思考边讲解思路的习惯，代码写完主动进行测试用例验证。
  4. **反馈总结**（10分钟）：模拟结束后请朋友评价你的表现，尤其在哪些问题上回答不够到位。及时针对反馈查漏补缺，完善相应知识。
     通过高仿真的演练，提前暴露问题并调整，应对真实面试时将更加自信从容。

## Day 13: 面试冲刺 – 重难点强化与心理调适

- **理论学习目标：** 在临近面试之际，针对自身还不踏实的知识点进行最后强化记忆，并调整心态迎接面试。温习前几日整理的笔记，确保细节（公式、年限、原理）都熟记于心。同时准备好案例和问题，形成自己的**面试答题模板**。
- **重点巩固内容：**
  - **公式细节：** 再次书写重要公式：Logloss定义fancyerii.github.io、AUC计算方法、FM的二阶项公式、注意力权重公式等，如有模糊赶紧复习牢记。
  - **模型细节：** 回顾每个模型的输入输出维度、激活函数（如DSSM原论文用tanh激活[cnblogs.com](https://www.cnblogs.com/arachis/p/DSSM.html#:~:text=1)）、损失函数等容易被问到的细节。想象面试官可能让你画出模型结构或流程图，在草稿纸上练习画一下DeepFM和DIN结构示意，确保讲解时层次清楚。
  - **业务结合：** 思考算法如何与业务目标挂钩的例子，例如“如果要在推荐中增加新颖性或多样性怎么办？”准备一些常见解决方案（比如结果重排序、增强探索）。对于简历中的项目，扩展考虑业务意义和可落地性，如你做的算法如何提升了某关键指标（留存率、点击率），有没有A/B Test验证。
  - **常见陷阱题：** 一些面试爱问的开放题，如“如何防止点击率预估中的数据偏差？”（可以谈曝光偏差、位置偏差，提到IPS校正或因果方法）；“推荐系统如何评估？”（离线指标AUC、线上A/B测试、用户满意度调查等）；还有“长尾物品如何处理”、“实时性要求如何实现”等，将前面学的知识应用其中准备回答。
- **心理和策略准备：**
  - **心态调整：** 适度焦虑是正常的，但要避免疲劳。当日可回顾此前模拟面试中表现出的不足，提醒自己改正。同时告诉自己这两周已经系统准备过，对常考点心里有底，给自己积极暗示增强信心。
  - **面试礼仪：** 准备好面试要用的纸笔、电脑环境。练习通过视频表达时的音量和语速，确保听清问题后先思考再回答，不急于抢答。
  - **时间规划：** 确定好面试当天的行程，提前测试网络和设备。准备一份简短的“感谢说明”，在面试结尾如有机会表达对面试官提问的感谢和对岗位的热情。
- **实战任务：** **最后检查与准备：**
  1. **知识点随机抽查：** 把制作的知识点纸条打乱，抽取5-10个进行快速解释，看能否流畅回答。如果有卡壳的，再重点记忆5分钟再复述。
  2. **模拟陌生提问：** 上网找最近的推荐系统面试经历帖子，选一道自己没准备过的问题模拟回答，如“协同过滤和矩阵分解的区别”。训练自己在未知问题前的分析能力和思维框架。
  3. **整理资料：** 将需要牢记的数据（比如某论文发表年份、某模型指标提升多少）再浏览一遍，避免在面试叙述项目时数字张冠李戴。
  4. **良好休息：** 保证充足睡眠，使头脑清晰。面试前适当做深呼吸或听舒缓音乐放松，保持自信从容的精神状态。
     至此，技术、经验和心态都已准备就绪，只待明天在面试中充分展现。

## Day 14: 面试当日流程与经验积累

- **面试进行：** 今天就是面试日，按照计划从容应对：提前进入会议室或在线等待。**自我介绍**时简洁强调与岗位相关的技能（深度学习、推荐项目经验），**技术问答**环节冷静聆听问题、条理回答，不会的问题坦诚思考过程。遇到需要白板演示时，先列提纲再展开讲解，确保逻辑清晰。编码题目则注意边思考边解释，让考官了解你的解题思路。**保持自信和求知欲**，表现出你对推荐/搜索领域的热情和持续学习的意愿。
- **注意事项：** 切记面试不仅考查知识，更看重**思考问题的方法**和**沟通表达**。因此回答时尽量结构化，例如被问到模型对比，先列出比较维度（结构、优缺点、应用场景），再逐点阐述。对待压力提问，保持礼貌，从容应对。
- **面试结束：** 面试官通常会询问你有没有问题，可准备1-2个有深度的问题（比如“贵司推荐算法如何兼顾新用户体验？”），展现你的兴趣。结束时礼貌致谢。
- **事后总结：** 无论结果如何，面试后立即记录下题目和自己的回答回顾，权当又一次学习积累。如果有感觉没答好的地方，标记出来日后加强。这样即使此次不理想，也为下次打下更坚实的基础。
- **持续学习：** 推荐和搜索领域新论文新技术层出不穷，面试准备虽告一段落，但建议保持学习习惯，关注顶会RecSys/KDD及业界博客，跟进行业动态。这份两周计划也可在今后作为模板，根据需要扩展延伸。

**最终，祝你在推荐与搜索算法岗位的面试中取得优异表现！**通过14天系统而高效的准备，你已经掌握了CTR预估、推荐排序、召回及语义搜索等核心知识，并具备了一定实战经验和面试技巧。相信这些积累将帮助你从容应对技术面试，成功拿到心仪的Offer！[cnblogs.com](https://www.cnblogs.com/zhengzhicong/p/12862816.html#:~:text=最后感叹一句，深度学习CTR模型的发展实在是太迅速了，很多新模型就不继续介绍了，要保持学习啊！)[blog.csdn.net](https://blog.csdn.net/2401_86652632/article/details/146373614#:~:text=)

# 二、

## Day 1｜从0到1搭一条极简推荐流程（MovieLens）

**目标**：体验“召回→排序→评估”的最小闭环。
 **数据**：MovieLens 1M 或 100K（ml-1m.zip / ml-100k.zip）。

1. 环境

- `conda create -n recsys python=3.10 -y && conda activate recsys`
- `pip install pandas numpy scikit-learn matplotlib`

1. 数据准备

- 解压到 `data/ml/`；读取 `ratings.dat`（或 `u.data`），列名统一为：`user_id, item_id, rating, timestamp`。
- 构造**隐式反馈**：`label = 1`（评分≥4），负样本用同用户未交互的物品随机采样若干条（label=0）。

1. 召回（简单热门/ItemCF 二选一）

- 热门召回：统计全局最热门物品TopN做候选。
- 或 ItemCF：构建物品共现矩阵（同一用户看过即+1），按余弦相似度给用户召回相似于其最近看过的电影TopN。

1. 排序（Logistic Regression）

- 特征：`[user_id, item_id, item_popularity]`（ID用LabelEncoder转数值）。
- 模型：`sklearn.linear_model.LogisticRegression`；训练集/验证集=8:2。
- 指标：AUC、logloss。

1. 预测与评估

- 对每个用户输出TopK（K=10）并计算**命中率/Recall@K、NDCG@K**（可先实现Recall@K）。
- 保存：`outputs/day1_topk.csv`。

------

## Day 2｜Criteo小样本上做 LR / FM（CTR入门）

**目标**：跑通传统CTR链路，感受特征工程影响。
 **数据**：Criteo（取前20万行做子集）。

1. 环境

- `pip install pyarrow scipy tqdm`

1. 数据预处理

- 数值特征：缺失填0；截断长尾（如clip到P99）。
- 类别特征：高基数截尾（频次<10置为“**OTHER**”）；LabelEncoder。
- 训练/验证分割：按时间或随机（80/20）。

1. 基线LR

- 特征拼接为稀疏向量（可用`scipy.sparse.hstack`）。
- 训练`LogisticRegression(solver='liblinear' 或 'saga')`。
- 记录AUC/Logloss。

1. FM（二阶交叉）

- 选用轻实现（如`pip install polylearn`）或自己写FM：
  - 公式：$\hat{y}=w_0+\sum w_ix_i+\sum_{i
  - 训练：SGD/Adam；Embedding维度k=8/16。
- 比较与LR的AUC/Logloss。

1. 日志与可视化

- 保存训练曲线、对比表`outputs/day2_metrics.csv`。

------

## Day 3｜DeepFM（用DeepCTR-Torch快速起步）

**目标**：端到端CTR深度模型。
 **数据**：继续用Criteo子集。

1. 环境

- `pip install torch deepctr-torch sklearn pandas`

1. 特征列定义（DeepCTR风格）

- 把类别列包装成 `SparseFeat(name, vocabulary_size, embedding_dim)`；
- 数值列包装成 `DenseFeat(name, 1)`。

1. 代码骨架

```
from deepctr_torch.inputs import SparseFeat, DenseFeat, get_feature_names
from deepctr_torch.models import DeepFM

linear_features = [...]
dnn_features = [...]
model = DeepFM(linear_features, dnn_features, task='binary')
model.compile("adam", "binary_crossentropy", metrics=['auc'])
model.fit(train_model_input, y_train, batch_size=4096, epochs=3, validation_data=(val_model_input, y_val))
```

1. 关键超参

- `embedding_dim=8/16`，`dnn_hidden_units=[256,128,64]`，`dropout=0.2`。

1. 评估与对比

- 输出AUC/Logloss与Day2比较；保存在`outputs/day3_deepfm_report.json`。
- 导出特征重要性（可通过打乱单特征看AUC降幅近似）。

------

## Day 4｜DIN（行为序列注意力）

**目标**：把“候选item × 用户历史行为”做注意力加权。
 **数据**：阿里天池电商数据（或用你自己的日志构造`user_id, item_id, hist_items[]`）。

1. 环境

- `pip install deepctr-torch`（其内置DIN实现）
- 或自写轻量DIN：Embedding → 注意力(候选item对hist_items打分) → 加权和 → MLP。

1. 序列构造

- 对每个样本，截取用户最近`L=50`个历史点击item（不足padding 0）；
- 候选item为当前训练样本的`item_id`；标签label=是否点击/购买。

1. 用DeepCTR-Torch DIN

```
from deepctr_torch.models import DIN
# 行为域：['item_id']，对应hist序列：['hist_item_id']
behavior_feature_list = ['item_id']
model = DIN(dnn_feature_columns, behavior_feature_list, ...)
```

1. 训练与注意力可视化

- 训练3-5个epoch；
- 抽样一个用户、一个候选item，取出注意力权重（库里可通过hook或调试变量）；
- 保存`outputs/day4_attn_weights.json`。

1. 与DeepFM对比

- 观察AUC/Recall@K变化；记录差异。

------

## Day 5｜召回对比：UserCF vs 向量召回（Faiss）

**目标**：体会“快而全”的召回。
 **数据**：MovieLens或你的点击日志。

1. 环境

- `pip install faiss-cpu`（Windows用`faiss-cpu`轮子）

1. UserCF召回

- 构建用户×物品稀疏矩阵；计算用户相似（余弦）；
- 对目标用户取最相似的M个用户的Top物品 → 合并去重 → TopN 作为候选。

1. 向量召回（item2vec 或 MF）

- **item2vec**：把每个用户序列当句子，训练skip-gram（`gensim`或自写负采样）；
- 得到`item_emb`，用户向量=最近K个item_emb平均。
- 建Faiss索引：`index = faiss.IndexFlatIP(dim)`；
- `index.add(item_emb_matrix)`；查询用户向量：`index.search(u_emb, topk)`。

1. 评估

- 对同一用户集合，统计两种召回的**覆盖率、召回率**；
- 保存`outputs/day5_recall_compare.csv`。

------

## Day 6｜DSSM 双塔文本匹配小实验（LCQMC / Quora）

**目标**：实现可训练的“双塔 + 对比学习”。
 **数据**：LCQMC（中文句对相似）或Quora duplicate questions。

1. 环境

- `pip install torch transformers datasets sentencepiece scikit-learn`

1. 数据处理

- 读取 (q1, q2, label)；正样本为label=1；负样本可用**in-batch negatives**（同batch内不同q2作负例）。

1. 模型结构

- **塔共享参数**：`Encoder = BERT/RoBERTa`（或轻量TextCNN）；
- `h1 = Enc(q1)[:, CLS]`，`h2 = Enc(q2)[:, CLS]`；
- 相似度：`sim = h1 @ h2.T`；
- 损失：InfoNCE / 对比损失（正对角为正样本）。

1. 训练示例（伪代码）

```
# batch: q1_ids, q2_ids
h1 = enc(q1_ids).pooler_output   # [B, D]
h2 = enc(q2_ids).pooler_output   # [B, D]
logits = h1 @ h2.T / tau         # [B,B]
labels = torch.arange(B)         # 期望对角最大
loss = cross_entropy(logits, labels)
```

1. 检索测试

- 把全部候选q2向量编码入库；
- 给定查询q1，用内积TopK检索最相似q2；手动检查若干例子。
- 保存向量库与检索结果。

------

## Day 7｜BERT语义检索：FAQ Demo（Bi-Encoder + Faiss）

**目标**：把Sentence-BERT嵌入落地到FAQ检索。
 **数据**：自建10-50条FAQ（问题-答案对，中文）。

1. 环境

- `pip install sentence-transformers faiss-cpu`

1. 索引构建

```
from sentence_transformers import SentenceTransformer
import faiss, numpy as np
model = SentenceTransformer('sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2')
faq_qs = [...]  # 列表
X = model.encode(faq_qs, normalize_embeddings=True)
index = faiss.IndexFlatIP(X.shape[1]); index.add(X.astype('float32'))
```

1. 查询

```
q = "账号忘记密码怎么办"
qv = model.encode([q], normalize_embeddings=True).astype('float32')
D,I = index.search(qv, k=5)  # 返回Top5
```

1. 与关键词对比

- 同一查询用`difflib`或简单TF-IDF做文本检索，对比Top1是否命中正确FAQ。
- 保存`outputs/day7_faq_search.csv`。

------

## Day 8｜小型“搜推一体化”原型（统一向量）

**目标**：同一套向量用于**推荐（无查询）\**和\**搜索（有查询）**。
 **数据**：新闻或文章集合（title+content+category），外加若干用户的阅读历史（可随机生成）。

1. 环境

- 复用Day7的`SentenceTransformer` + `faiss-cpu`。

1. 文章库向量化

- `item_emb = model.encode([title_or_concat], normalize=True)`；
- 建Faiss索引。

1. **推荐**逻辑

- 对每个用户取最近N篇已读文章embedding，求平均/加权平均得到`user_emb`；
- 在文章索引中TopK检索 → 推荐结果；
- 过滤已读。

1. **搜索**逻辑

- 把用户输入Query编码为`q_emb`；
- 在同一索引中检索；
- **混排策略**（可选）：`score = α·(q⋅item) + (1-α)·(user⋅item)`，调α=0.5/0.7对比效果。

1. 评估

- 构造少量人工标注：某些query对应正确文章集合；
- 用命中率/精排Top1准确率粗评；
- 保存demo脚本与结果。

------

## Day 9｜比赛实战（I）：数据管线与特征（天池/Avazu/Criteo）

**目标**：像参赛一样搭**可复用**的数据流水线。
 **数据**：任选一个CTR/推荐比赛数据集。

1. 目录结构

```
project/
  data/
  src/
    preprocess.py
    train_lr.py
    utils_metrics.py
  outputs/
```

1. 数据探索

- `preprocess.py`：输出缺失率、类别基数TopN、数值分布直方图（`matplotlib`）。

1. 特征工程

- ID截尾、时间特征提取（小时/星期）、交叉特征草测（如`user×category` hash）；
- 保存`train.parquet`、`valid.parquet`。

1. Baseline

- `train_lr.py`：LR 或 LightGBM（`pip install lightgbm`），输出AUC/Logloss、特征重要性。

1. 版本管理

- 所有超参写到`config.json`，训练时保存`outputs/run_xxx/metrics.json`与`feature_importance.csv`。

------

## Day 10｜比赛实战（II）：DeepFM + DIN + 融合

**目标**：逐步加料提升指标。
 **数据**：沿用Day9。

1. DeepFM 训练

- 用DeepCTR-Torch或自写PyTorch版本；
- 记录：AUC/Logloss曲线、早停（patience=2~3）。

1. 加DIN（如存在序列）

- 序列构造脚本 `build_sequence_features.py`；
- Ablation：只DeepFM vs DeepFM+DIN。

1. 模型融合

- 训练一个 LightGBM 输出`pred_lgb.csv`；
- 与 DeepFM 输出`pred_dfm.csv`做加权：`p = 0.6*dfm + 0.4*lgb`；
- 选择最优权重（网格搜索0.1步长）。

1. 离线评估 +（可选）线上提交

- 统一评估脚本：`evaluate.py --pred submission.csv --label valid.csv`；
- 保存`outputs/day10_summary.md`记录每次改动与得分。

------

## Day 11｜系统复盘产物（图 + 表 + 模板答案）

**目标**：把“能跑”变成“能讲、能画、能复用”。
 **产物清单**：

1. **一页图**：搜推一体化体系图（召回/粗排/精排/重排、向量库、Faiss、特征平台）。
2. **一页表**：模型对比（LR/FM/DeepFM/DIN/DSSM/BERT Bi-Enc/Cross-Enc），列模型结构、优缺点、延迟级别、典型应用。
3. **三段模板答案**：
   - “DeepFM为何优于Wide&Deep（免人工交叉）”；
   - “DIN如何动态激活兴趣（候选条件化的Attention）”；
   - “双塔为何适合召回（离线物品向量+在线user向量+ANN）”。

**实现步骤**：

- 用`draw.io`或`pptx`画图（保存到`outputs/day11_arch.pptx`）；
- 表格用`pandas.DataFrame.to_markdown()`生成；
- 把模板答案放`outputs/cheatsheet.md`。

------

## Day 12｜模拟面试脚本 + 演练

**目标**：压缩表达 + 当场推演。

1. 写**3分钟自我介绍**脚本（含两处可插入图/表的点）。
2. 准备**10个高频问答**（每个不超150字+1张小图示意）。
3. 准备**1个系统设计题**答题框架（例如“短视频推荐系统”）：数据→召回（多路：CF/向量/规则）→排序（CTR/CVR/多目标）→重排与多样性→反馈与A/B。
4. 演练：开`obsidian`或`notion`计时演讲；修剪到时间内。
5. 录音复听，列出3个改进点并更新脚本。

------

## Day 13｜硬核查漏：指标/偏差/上线

**目标**：补工程&评估易被问到的坑。
 **步骤**：

1. **指标**：实现AUC、NDCG@K、Recall@K的`metrics.py`（手写版，别只会调用库）。
2. **偏差矫正**：写一页说明：曝光偏差（IPS/DR）、位置偏差（Position Bias）、数据泄漏（时间切分）。
3. **性能与上线**：
   - 写faiss在线检索的QPS与延迟估算表（向量维度、TopK、CPU核数）；
   - 写“BERT蒸馏到双塔”的简短方案（teacher cross-encoder → student bi-encoder；对比学习蒸馏）。
4. 把以上内容整理入`outputs/day13_engineering.md`。

------

## Day 14｜面试日工具包

**目标**：临门一脚，拿着就上场。

1. `one_pager.pdf`：把Day11的图/表 + 3段模板答案压一页。
2. `demo_notebook.ipynb`：打包Day7 FAQ语义检索demo（能当场跑Top5）。
3. `story_bank.md`：STAR故事各2个：
   - 提升AUC/解决过拟合/特征失效；
   - 做过召回大改造/延迟优化/线上事故复盘。
4. 快速热身：
   - 现场随手画DIN/DeepFM/双塔结构框图；
   - 10道概念闪答（自测 10×30秒）。

------

## 你可以直接拷贝的“最小代码模板”补充

**A. 评估指标（手写Recall@K / NDCG@K）**

```
import numpy as np

def recall_at_k(gt_items, rec_items, k=10):
    # gt_items: set或list，真实正样本集合
    # rec_items: list，模型TopK推荐
    rec_k = rec_items[:k]
    hit = len(set(gt_items) & set(rec_k))
    return hit / max(1, len(set(gt_items)))

def ndcg_at_k(gt_items, rec_items, k=10):
    rec_k = rec_items[:k]
    dcg = sum([1/np.log2(i+2) if rec_k[i] in gt_items else 0 for i in range(len(rec_k))])
    ideal = sum([1/np.log2(i+2) for i in range(min(len(gt_items), k))])
    return dcg / ideal if ideal > 0 else 0.0
```

**B. 简易Faiss向量召回**

```
import faiss, numpy as np

def build_index(item_emb):  # [N, D] float32 normalized
    d = item_emb.shape[1]
    index = faiss.IndexFlatIP(d)
    index.add(item_emb)
    return index

def topk(index, query_vec, k=20):  # query_vec [1, D]
    D, I = index.search(query_vec, k)
    return I[0].tolist(), D[0].tolist()
```

**C. DeepFM（纯PyTorch超简骨架，便于理解）**

```
import torch, torch.nn as nn

class FM(nn.Module):
    def __init__(self, num_feats, k):
        super().__init__()
        self.w0 = nn.Parameter(torch.zeros(1))
        self.w = nn.Parameter(torch.zeros(num_feats, 1))
        self.V = nn.Parameter(torch.randn(num_feats, k) * 0.01)
    def forward(self, x):  # x: [B, F]
        linear = self.w0 + (x @ self.w).squeeze(-1)
        xv = x @ self.V           # [B, k]
        x2v2 = (x**2) @ (self.V**2)
        inter = 0.5 * torch.sum(xv**2 - x2v2, dim=1)
        return linear + inter

class DeepFM(nn.Module):
    def __init__(self, num_feats, k, dnn_units=[256,128,64]):
        super().__init__()
        self.fm = FM(num_feats, k)
        dims = [num_feats] + dnn_units + [1]
        layers = []
        for i in range(len(dims)-1):
            layers += [nn.Linear(dims[i], dims[i+1])]
            if i < len(dims)-2:
                layers += [nn.ReLU(), nn.Dropout(0.2)]
        self.dnn = nn.Sequential(*layers)
    def forward(self, x):
        y_fm = self.fm(x)
        y_dnn = self.dnn(x).squeeze(-1)
        return torch.sigmoid(y_fm + y_dnn)
```