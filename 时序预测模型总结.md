# 1. 时序预测模型

## 1.1 Transformer模型和Informer模型原理

### 1.1.1 注意力机制和稀疏注意力机制。

​	注意力机制是一种模拟“人类聚焦关键区域”的思想，在序列建模中可根据不同输入的*相关性***动态分配注意力权重**，提升模型对远距离依赖的建模能力。传统的全连接注意力（如 Transformer 中使用的）计算量为 $O(L^2)$，在长序列中开销大。稀疏注意力机制（如 Informer 提出的 Prob Sparse Attention）通过筛选“重要的注意力对”，将复杂度降为 $O(L \log L)$，显著提高长序列建模效率。

​	注意力机制的核心原理是：**在处理一组输入时，模型可以根据每个输入的重要程度分配不同的“权重”，从而聚焦于最相关的信息**。具体来说，注意力机制通过“查询（Query）”与所有“键（Key）”计算相似度（通常用*点积*或相似函数），再用 soft max 将其转化为权重，最后将这些权重加权在对应的“值（Value）”上，生成最终输出。这种机制使得模型可以**动态感知序列中各部分之间的依赖关系**，尤其适合**捕捉长距离关联**，是 Transformer 等模型建模全局信息的基础。

### 1.1.2 Transformer和Informer的区别和联系？**

​	Transformer 模型由 **编码器-解码器结构（Encoder-Decoder）** 构成，核心模块包括多头注意力、前馈网络、残差连接与层归一化，**并行处理所有输入位置**，强于捕捉全局依赖。	   Informer 则是专为长时间序列预测设计的改进模型，它在 Transformer 基础上引入 **稀疏注意力机制**、**预测长度延伸（long-sequence prediction）** 和 **可学习的时间编码器**，在降低计算成本的同时保留全局表达能力。

​	两者的核心区别在于：Transformer 适用于**中短序列的泛用建模任务**，如 N LP、代码理解等；Informer 针对**长序列、高频多步时间预测**任务进行了优化，适用于如电力负荷预测、气象预测、工业监测等场景。两者都依赖注意力机制，但 Informer 在结构设计上更强调 **高效性与预测性能平衡**。

​	Informer 模型在 Transformer 的基础上进行了三项关键改进，以适应长时间序列预测任务：① 引入 **稀疏自注意力机制（Prob Sparse Attention），显著降低了原始 Transformer $O(L^2)$ 的计算复杂度到 $O(L \log L)$，提升处理长序列的效率；② 采用长序列预测机制（Long-sequence Prediction），支持一次性并行输出多步预测结果，避免传统 Transformer 自回归预测的低效问题；③ 加入可学习的时间编码与解码器结构调整**，增强模型对周期性、趋势性时间特征的感知能力。这些改进使得 Informer 更适合用于高频、长序列、多变量的时间序列预测场景。

​	Informer 用**可学习的时间编码代替固定位置编码**，使模型更灵活地**理解时间规律**；同时调整了解码器结构，使其能在预测阶段**一次性并行输出所有未来时间点**，提升预测效率并减少误差传播。

## 1.2 Prophet模型原理

### 1.2.1 Prophet与Informer的联系和区别？

|   比较维度   |                     Informer（深度模型）                     |                Prophet（统计模型）                 |
| :----------: | :----------------------------------------------------------: | :------------------------------------------------: |
|   模型类型   |               深度学习模型（基于 Transformer）               | 加性时间序列模型（Trend + Seasonality + Holidays） |
| 时间信息建模 | 使用**可学习的时间特征**嵌入（年、月、日、节假日等），通过 Attention 学习时间依赖 |  显式添加季节性项、节假日项，时间特征建模明确可控  |
|   数据处理   |           支持多变量、非线性、上下文交互复杂的输入           |     仅支持单变量时间序列（如销售量随时间变化）     |
| 长期依赖建模 |         使用稀疏注意力捕捉远距离依赖，适合长序列预测         |    趋势+周期建模结构有限，无法建模复杂长期依赖     |
|   训练方式   |              需大数据训练，损失函数反向传播优化              |       拟合趋势与周期性曲线，无需深度训练过程       |
|   精度表现   |           在大样本 + 多变量 + 非线性场景下通常更优           |         在小样本 + 稳定周期性场景下更稳定          |
|   可解释性   |    attention 机制有一定可解释性（但需分析），整体是“黑盒”    |       可视化趋势、周期、节假日效果，解释性强       |
|   推理效率   |       推理速度快（一次性输出），适合部署在服务端或边缘       |         非并行但计算量小，适合快速原型搭建         |

它们在“时间信息建模”上的**联系**：

| 时间特征维度    |             Informer 用法             |             Prophet 用法             |
| --------------- | :-----------------------------------: | :----------------------------------: |
| 年、月、日      |          作为 embedding 学习          |       作为周期性（年周期）建模       |
| 小时、分钟      |      用于高频数据的时间位置嵌入       |     支持“每日”季节性，但粒度有限     |
| 是否节假日/周末 |        加入节日 flag embedding        |       显式加入 holiday 效应项        |
| 时间先后顺序    | 由位置编码（Positional Encoding）提供 | Prophet 自带趋势项（线性或饱和增长） |

如何**选择**：

|             应用场景             |            推荐模型             |                       理由                        |
| :------------------------------: | :-----------------------------: | :-----------------------------------------------: |
| 单变量预测 + 有明显周期 + 小样本 |             Prophet             |   快速、可解释、无需深度训练，适合业务原型开发    |
|    多变量输入 + 长期依赖建模     |            Informer             | 能捕捉变量间交互，注意力机制能处理复杂的时序关系  |
|       目标是中长期趋势预测       | Prophet 可先用，Informer 可微调 | Prophet 做趋势建模基础，Informer 精调或多变量增强 |
|  部署在端侧 / 对推理性能有要求   |            Informer             |           一次性并行预测、适合边缘部署            |
|        K P I 要求精度最优        |      Informer（+ tuning）       |         支持多步预测，精度远高于统计模型          |

1. ***LSTM和prophet有什么区别和联系？如何根据要解决的问题进行选择呢？***

   LSTM 与 Prophet 的核心区别：

   |  对比维度  |            **LSTM**（深度学习模型）             |                **Prophet**（统计模型）                |
   | :--------: | :---------------------------------------------: | :---------------------------------------------------: |
   |  模型类型  |   基于 RNN 的循环神经网络，擅长非线性时序建模   | 加性模型：趋势 + 周期 + 节假日 + 残差（由 Meta 提出） |
   |  记忆机制  |    拥有长期记忆单元，能捕捉长时间跨度的依赖     |          无记忆机制，仅对“已知周期趋势”建模           |
   |  输入特征  |  多变量输入（历史值、外部变量），需归一化处理   |   仅支持单变量（即目标变量），时间是唯一的输入特征    |
   |  输出能力  |        可灵活预测单步、多步、回归或分类         |    原生支持多步预测（带置信区间），适合趋势性预测     |
   | 非线性能力 |        强，能拟合复杂非线性和变量间交互         |            弱，只能建模简单线性趋势 + 周期            |
   |  可解释性  | 弱，结果为“黑盒”，需配合 SHAP 或 attention 分析 |          强，分量清晰（趋势、季节、假期等）           |
   | 预测稳定性 |          易受噪声影响，需正则化、调参           |               对稳定周期性数据非常稳健                |
   |  训练代价  |            需要 GPU 训练、调参、验证            |                无需训练，快速拟合即可                 |
   |  推理效率  |        中等偏高（依赖 batch、模型大小）         |                快速，可部署到轻量系统                 |

   联系：都用于时间序列预测，但建模路径不同：

   |            相同点            |                     说明                     |
   | :--------------------------: | :------------------------------------------: |
   |     都基于时间为核心变量     |              时间是预测驱动因素              |
   |       都可预测未来数值       |              支持单步或多步预测              |
   | 都可集成节假日、季节性等信息 | 但 Prophet 显式支持，LSTM 需自行设计输入特征 |
   |     都可用于业务趋势建模     |     如销量预测、负载预测、温度预测等场景     |

   ​	Prophet 适合快速、解释性强的**单变量**趋势预测任务；LSTM 适合多变量、非线性、依赖强、精度要求高的复杂预测任务。

## 1.3LSTM模型原理

***1.时序预测问题的核心挑战***

​	时序数据（如股票价格、自然语言、气象数据等）的核心特点是 **“时间依赖性”**：当前的***输出不仅依赖于当前的输入，还依赖于过去多个时间步的输入***（甚至是长期的历史信息）。例如：

- 预测句子的下一个单词时，需要依赖前面几句话的语义（长期依赖）；

- 预测明天的气温时，需要参考过去一周的温度趋势（中期依赖）和季节规律（长期依赖）。

  因此，时序预测的核心挑战是：**如何有效捕捉并利用序列中 “短期” 和 “长期” 的依赖关系**。

***2.普通神经网络为何无法解决时序预测问题？***

普通神经网络（如全连接网络、简单 RNN 等）由于结构限制，无法满足时序预测的需求，具体原因如下：

1. 全连接神经网络（Feedforward NN）：完全忽略时间结构

全连接网络的输入是**固定长度的向量**，且输入之间没有 “时间顺序” 的概念。例如：

- 若用全连接网络预测句子的下一个单词，需要将前 N 个单词拼接成固定长度的向量作为输入，但网络无法理解 “单词的顺序”（比如 “我吃苹果” 和 “苹果吃我” 对它来说输入向量相似，但语义完全相反）；
- 对于变长时序数据（如不同长度的句子、不同天数的温度序列），全连接网络无法直接处理（需强行截断或补全，破坏时序完整性）。

本质上，全连接网络是 “无记忆” 的 —— 它将每个输入视为独立样本，完全忽略时间维度的依赖关系。

2. 简单循环神经网络（Simple RNN）：无法捕捉长期依赖

​	简单 RNN 是为处理时序数据设计的（通过循环结构保留 “记忆”），但存在致命缺陷：**梯度消失 / 爆炸问题**，导致无法学习长期依赖。

​	简单 RNN 的结构是 “循环” 的：当前时刻的输出不仅依赖于当前输入，还依赖于上一时刻的 “隐藏状态”（可理解为 “记忆”）。其隐藏状态更新公式为：
$$
h_t​=σ(W_{ih}​x_t​+W_{hh}​h_{t−1}​+b_h​)
$$

$$
其中h_t是当前隐藏状态，h_{t-1}是上一时刻的隐藏状态，W_{hh}是隐藏状态的权重矩阵。
$$

​          当训练时，模型通过反向传播更新参数，而梯度需要从当前时刻反向传递到历史时刻（例如计算h_t对h_0的梯度）。此时梯度的计算会涉及W_{hh}的多次乘法（如h_t对h_0的梯度与W_{hh}^t相关）：

- 若\(W_{hh}\)的特征值小于 1，多次乘法后梯度会 “指数级衰减”（梯度消失），导致早期时刻的信息无法影响后期参数更新（比如句子开头的信息无法影响结尾的预测）；
- 若\(W_{hh}\)的特征值大于 1，梯度会 “指数级增长”（梯度爆炸），导致参数更新不稳定。

​            因此，简单 RNN 只能捕捉**极短的短期依赖**（如几句话内的关系），无法处理长时序数据（如几百个时间步的序列）。

***3.LSTM 模型：通过 “门控机制” 解决长期依赖***

​	LSTM（长短期记忆网络）是 RNN 的变体，其核心创新是通过 **“门控机制” 和 “细胞状态”** 解决了**梯度消失**问题，从而能有效捕捉**长期依赖**。下面详细解析其结构和原理。

​	**1.LSTM 的核心设计：细胞状态与门控**

​	LSTM 的核心是一个 ***“细胞状态”（cell state）***，它类似一条 “信息传送带”，信息在上面线性传递（几乎无衰减），这是保留长期信息的关键。同时，LSTM 通过三个 “门”（遗忘门、输入门、输出门）控制信息的 “丢弃”“输入” 和 “输出”，实现对信息的***选择性处理***。

​	**2. 四大核心组件的作用（附公式与直观解释）**

​	LSTM 的每个时间步t的计算涉及以下组件（假设输入为\(x_t\)，上一时刻隐藏状态为\(h_{t-1}\)，上一时刻细胞状态为\(c_{t-1}\)）：

（1）遗忘门（Forget Gate）：决定 “忘记” 哪些历史信息

​	遗忘门的作用是筛选需要从细胞状态中**丢弃的旧信息**，通过 sigmoid 函数输出 0-1 之间的值（1 表示完全保留，0 表示完全丢弃）。

公式：
$$
f_t​=σ(W_f​⋅[h_{t−1}​,x_t​]+b_f​)
$$
其中\W_f\是遗忘门的权重矩阵，\[h_{t-1}, x_t]是上一时刻隐藏状态与当前输入的拼接，\sigma\是 sigmoid 函数（输出 0-1）。

直观理解：

- 在文本预测中，若当前讨论的主题从 “天气” 切换到 “美食”，遗忘门会输出接近 0 的值，丢弃之前关于 “天气” 的信息；
- 在股票预测中，若某条突发新闻（如政策变动）对长期趋势无影响，遗忘门会丢弃该信息，保留长期市场规律。

（2）输入门（Input Gate）：决定 “新增” 哪些信息

​	输入门负责筛选需要加入细胞状态的新信息，分为两步：

- 第一步（sigmoid 层）：决定 “哪些位置需要更新”（输出 0-1）；
- 第二步（tanh 层）：生成 “候选更新值”（范围 - 1 到 1，作为新信息的候选）。

公式：
$$
i_t = \sigma(W_i \cdot [h_{t-1}, x_t] + b_i) \quad \text{（决定更新位置）} \\\tilde{c}_t = \tanh(W_c \cdot [h_{t-1}, x_t] + b_c) \quad \text{（候选更新值）}
$$

（3）细胞状态更新：保留重要旧信息 + 加入新信息

​	细胞状态是 LSTM 的 “*记忆核心*”，其更新规则是：用***遗忘门保留有价值的旧信息***，用输入门加入***筛选后的新信息***。

公式：
$$
c_t = f_t \odot c_{t-1} + i_t \odot \tilde{c}_t\\ 其中\odot是元素乘法。
$$
​	关键特性： 细胞状态的更新是**线性操作**（无复杂非线性变换），信息在 “传送带” 上几乎无衰减地传递。这使得长期信息（如句子开头的主题、季节周期规律）能稳定保留，从根本上缓解了简单 RNN 的梯度消失问题（梯度可沿细胞状态的线性路径稳定反向传播）。

（4）输出门（Output Gate）：决定当前输出什么

​	输出门控制 “*基于当前细胞状态，输出哪些信息”*，分为两步：

- 第一步（sigmoid 层）：决定 “细胞状态的哪些部分会被输出”；
- 第二步：用 tanh 处理细胞状态（将值压缩到 - 1 到 1），再与 sigmoid 的输出相乘，得到当前的隐藏状态\(h_t\)（即当前输出）。

公式：
$$
o_t = \sigma(W_o \cdot [h_{t-1}, x_t] + b_o) \quad \text{（决定输出部分）} \\h_t = o_t \odot \tanh(c_t) \quad \text{（当前隐藏状态/输出）}
$$
直观理解：

- 在文本生成中，若细胞状态保留了 “主语是单数” 的信息，输出门会选择性输出该信息，确保动词用 “is” 而非 “are”；
- 在温度预测中，细胞状态保留了 “冬季寒冷” 的长期规律，输出门会结合当前输入（如近期降温），输出合理的预测值。

***4.LSTM 解决时序预测问题的核心逻辑***

时序预测的核心是 “捕捉长期依赖”，而 LSTM 通过以下设计针对性解决了这一问题：

1. **细胞状态的线性传递**：信息在细胞状态中几乎无衰减地流动，确保长期信息（如几个月前的季节规律）能传递到当前时刻；
2. **门控机制的选择性处理**：遗忘门丢弃无关旧信息、输入门筛选关键新信息、输出门控制当前输出，使得模型能 “智能记忆”—— 只保留对预测有用的时序依赖；
3. **缓解梯度消失**：细胞状态的线性更新让梯度能稳定反向传播（无需经过多层非线性变换），解决了简单 RNN 无法学习长序列依赖的问题。

# 2.FastAPI介绍

## 2.1 fastapi的原理和特点以及能实现的功能？**

​	FastAPI 是一个基于 Python 3.6+ 的高性能 Web 框架，专为构建 API 接口而设计，底层依赖 Starlette（异步 Web 框架）和 Pydantic（数据验证库）。它通过 Python 的***类型注解机制***实现自动请求校验、参数解析和文档生成，并采用异步编程（async/await）支持高并发处理，性能媲美 Node.js 和 Go。

​	FastAPI 最大的特点是开发效率高、自动生成 Swagger 和 ReDoc 文档、类型安全强、兼容标准的 OpenAPI 规范。它可用于构建 RESTful API、微服务系统、异步任务接口、机器学习模型部署接口等，是现代 Python API 开发的首选框架之一。

# 3.神经网络

## 3.1 优化器

### 3.1.1 Adam优化器

​	Adam优化器是一种常用的自适应优化算法，结合了动量和自适应学习率调整的优点。以下是Adam优化器的一些主要特点：

1. 动量（Momentum）: Adam优化器利用了动量的概念，通过引入一阶动量（即参数梯度的指数加权平均），帮助模型加速收敛并减少震荡。
2. 自适应学习率（Adaptive Learning Rate）: Adam优化器通过引入二阶动量（即参数梯度平方的指数加权平均），为每个参数赋予不同的学习率，从而在不同方向上进行自适应调整。这使得Adam特别适合处理稀疏梯度和非平稳目标函数。
3. 计算效率高: Adam优化器只需要计算梯度的一阶矩和二阶矩，因此在计算上相对高效。
   适用于非平稳目标:由于Adam优化器能够自适应地调整学习率，它在处理非平稳目标函数时表现良好，能够有效应对目标函数形状的变化。
4. 参数较少: Adam优化器只需要两个额外的超参数（β1和β2）来控制动量的衰减和二阶矩的衰减，相比于其他复杂的优化算法，参数较少，使用起来更为便捷。
   鲁棒性好: Adam优化器通常不需要对初始学习率进行精细调优，这使得它在实际应用中更为鲁棒。
5. 适用于大规模数据和高维度空间: 由于其自适应学习率的能力，Adam优化器在大规模数据集和高维度参数空间中表现良好。
   	总之，Adam优化器因其自适应学习率、动量机制和高效性等优点，在许多深度学习任务中被广泛采用。它能够帮助模型更快地收敛，并且在不同的应用场景中表现出较好的性能。

## 3.2 数据处理

### 3.2.1 归一化
