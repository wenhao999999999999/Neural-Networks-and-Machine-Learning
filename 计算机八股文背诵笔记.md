# 1.计算机网络

## 1.什么是MAC地址？作用是什么？

​	MAC 地址（Media Access Control Address），即媒体访问控制地址，也被称为物理地址或硬件地址，是网络设备（如网卡、路由器接口等）出厂时由厂商烧录在设备硬件中的唯一标识符。

​	MAC 地址由 6 组十六进制数组成，每组 2 个字符，各组之间通常用冒号（:）或连字符（-）分隔，例如`00:1A:2B:3C:4D:5E`或`00-1A-2B-3C-4D-5E`。从结构上看，前 3 组（24 位）是**厂商识别码**（由 IEEE 分配给设备制造商），后 3 组（24 位）是**设备序列号**（由厂商自行分配），因此全球范围内的每个网络设备的 MAC 地址都是唯一的。

​	MAC 地址的核心作用是在**数据链路层**（OSI 模型第二层）标识网络设备，确保数据能在局域网内准确传输，具体包括以下几个方面：

1. **局域网内的身份标识**
   在同一局域网（如家庭 WiFi、公司内网）中，设备之间的通信依赖 MAC 地址。当一台设备向另一台设备发送数据时，数据帧中会包含目标设备的 MAC 地址，网络中的交换机等设备通过识别 MAC 地址，将数据准确转发到目标设备，避免数据发送到错误的终端。
2. **数据链路层的寻址**
   与 IP 地址（网络层地址，用于跨网络通信）不同，MAC 地址是 “本地” 地址，仅在当前局域网内有效。例如，当你的手机连接 WiFi 时，路由器会通过 MAC 地址识别手机，分配 IP 地址并建立通信链路。
3. **网络访问控制**
   许多网络（如企业内网、校园网）会通过 MAC 地址过滤来限制设备接入。管理员可以将允许接入的设备 MAC 地址加入 “白名单”，未在名单中的设备即使知道 WiFi 密码也无法连接，提升网络安全性。
4. **设备追踪与管理**
   由于 MAC 地址的唯一性，它可用于追踪网络中的设备。例如，路由器的管理界面会显示所有已连接设备的 MAC 地址，帮助用户识别陌生设备（如蹭网设备）并进行管理。
5. **交换机的转发依据**
   交换机通过维护 “MAC 地址表”（记录 MAC 地址与端口的对应关系）来高效转发数据。当交换机收到数据帧时，会根据目标 MAC 地址查找对应的端口，直接将数据发送到目标设备，避免广播风暴，提高局域网通信效率。

## 2.OSI(open system interconnection) 模型中网络层作用是什么？

### 逻辑地址寻址与标识

​	网络层使用 IP 地址来标识不同的主机和网络。与数据链路层用于局域网内硬件寻址的 MAC 地址不同，IP 地址是一种逻辑地址，具有全球唯一性，可用于跨网络通信。比如，在互联网中，每台连接的设备都有一个独立的 IP 地址，就像每个家庭都有一个唯一的门牌号， 方便信息准确送达。

### 数据路由与转发

- **路径选择**：当数据需要从源主机传输到目标主机时，如果源主机和目标主机不在同一个局域网，就需要经过多个网络设备（如路由器）进行转发。网络层会根据网络拓扑结构、链路状态、拥塞情况等因素，通过路由算法（如距离向量路由算法、链路状态路由算法 ）选择一条最佳路径。例如，在发送一封跨国电子邮件时，邮件数据会经过多个路由器的转发，网络层会计算出最优的传输路径，以确保数据快速、准确地到达目标邮箱服务器。
- **数据转发**：路由器是网络层的主要设备，它接收来自数据链路层的帧，提取其中的网络层数据（即*数据报*），根据路由表查找目标 IP 地址对应的下一跳地址，然后将数据报封装成新的帧，转发到下一个网络设备或目标主机。

### 网络互联与异构网络的整合

- **不同网络类型的连接**：网络层可以实现不同类型网络（如以太网、令牌环网、广域网等）之间的互联。例如，企业内部的局域网通过路由器与互联网相连，路由器在网络层对不同类型网络的数据进行转换和转发，使得企业内部设备能够访问外部网络资源，外部网络也能访问企业的特定服务（如企业网站）。
- **子网划分与管理**：通过子网掩码，网络层可以将一个大的网络划分为多个子网，便于网络管理和资源分配。比如，一个大型企业网络可以根据部门或地理位置划分为多个子网，每个子网有独立的 IP 地址范围，提高了网络的安全性和管理效率。

### 拥塞控制

​	当网络中数据流量过大，导致网络性能下降（如延迟增加、丢包率上升）时，网络层会采取拥塞控制措施。通过调整数据发送速率、丢弃部分数据报、向源主机发送拥塞通知等方式，缓解网络拥塞，保证网络的正常运行。例如，当某条网络链路因为用户大量下载视频而变得拥堵时，网络层会通过相关机制，控制发送端的数据发送速度，防止网络进一步拥塞。

### 差错检测与处理

​	虽然数据链路层也有差错校验功能，但网络层会对数据报在传输过程中可能出现的错误进行检测和处理。例如，当路由器接收到一个数据报时，会检查其头部信息是否正确，若发现错误，可能会丢弃该数据报，并通过 ICMP（互联网控制报文协议）向源主机发送错误报告，以便源主机重新发送数据。

